# Thesis A — sampled re-audit of `ok` rows (2026-09-05, second draw)

Files: `acstar-basic.csv`, `acstar-matrices-representation.csv`,
`acstar-positive.csv`, `acstar-towardsvn-avn-completeness.csv`,
`aproc-duplicators-quantumlambda.csv`, `aproc-measurement.csv`,
`aproc-tensor.csv`, `avn-basic.csv`, `avn-division-normalfunctionals.csv`,
`avn-projections.csv`.  All 130 rows of `SAMPLE-2026-09-05b.md` whose file
starts with `acstar-`, `avn-` or `aproc-`, re-derived from `cstar.tex`,
`vn.tex`, `proc.tex`, `asols.tex`, `ERRATA.md` and the Lean source.  The
rows' own `note` and `status` fields were not relied on.

Standards applied — `SAMPLE-BRIEF.md` plus the 2026-09-05 hint convention at
the end of `docs/STATEMENT-AUDIT.md`: an Exercise's printed **hint** counts as
a printed argument (`faithful` when followed, `route` when not); a point that
prints neither proof nor hint, with no solution and no erratum supplying one,
is `none` whatever the Lean does; the print may live at a sibling point or in
a solution keyed to the point, so that was checked before any `none` was
confirmed.  One further rule, carried over from the 2026-09-05 first draw and
applied uniformly here: **a private or auxiliary declaration that is not
itself the printed claim, and whose proof contains none of the printed
argument, is correctly `none`** even when the point it is filed under prints a
proof — the printed proof is graded on the sibling row that carries the claim.

**Counts: 120 CONFIRMED, 10 DEFECT, 0 UNSURE.**  All ten defects are in the
`proof` column; no sampled `stmt = ok` grade was found wrong.  The defect
lines are collected at the end of this file.

## `acstar-basic.csv` (9 rows)

- CONFIRMED — acstar-basic.csv:2, 3I `Mathlib CStarAlgebra / CommCStarAlgebra` (Theses/A/CStar/Basic.lean:22).  Print is the Definition at cstar.tex:84-127 (four items plus the closing commutativity sentence); Mathlib's `class CStarAlgebra` (Mathlib/Analysis/CStarAlgebra/Classes.lean:38, six parents) and `CommCStarAlgebra` (:42) carry all of them and nothing more.  A Definition prints no argument and `asols.tex` has no `parsec-30.10` key, so `none` is right.
- CONFIRMED — acstar-basic.csv:13, 4III `boundedOperators_basic_2` (Basic.lean:151).  Exercise cstar.tex:326 part 2, `‖ST‖ ≤ ‖S‖‖T‖`; solution asols.tex:511-516 is the one-line estimate `‖STx‖ ≤ ‖S‖‖Tx‖ ≤ ‖S‖‖T‖‖x‖`, and the Lean is that exact `calc` fed to `opNorm_le_bound`.
- CONFIRMED — acstar-basic.csv:51, 5VI `hilb_projection_basic_4` (Basic.lean:1126).  Last clause of cstar.tex:755-765 (`⟨y', x-y⟩ = 0` for `y' ∈ C`), siblings `_1`/`_2`/`_3` carrying the earlier clauses; the proof is the solution's final paragraph (asols.tex:984-991), not the Mathlib minimiser route the row's note once alleged.
- CONFIRMED — acstar-basic.csv:53, 5VII `projection_theorem_inner` (Basic.lean:1231).  Projection Theorem cstar.tex:766-772; its printed Proof (cstar.tex:774-778) says this clause comes from `hilb-projection-basic`, and the Lean derives it from `hilb_projection_basic_4`.  The unused `hC`/`[CompleteSpace H]` mirror the print's own hypotheses.
- CONFIRMED — acstar-basic.csv:85, 9X `cstar_positive_1_cone` (Basic.lean:2150).  cstar.tex:1209 part 1 (cone plus preorder), all five conjuncts stated; solution asols.tex:1228-1249 followed step for step (`0` via `‖0-0‖ ≤ 0`, sums via 9VII, scalars via `ofReal_smul_nonneg`, reflexivity and transitivity).
- CONFIRMED — acstar-basic.csv:91, 9X `orderNorm_le_norm` (Basic.lean:2436).  Part 4 of cstar.tex:1209; solution asols.tex:1289-1291 argues it from `-‖a‖ ≤ a ≤ ‖a‖` of part 2, and the Lean is `csInf_le` on `‖a‖ ∈ orderNormSet a` justified by `cstar_positive_2` — the solution's argument, not the deferred `orderNorm_eq_norm`.
- CONFIRMED — acstar-basic.csv:93, 9X `cstar_positive_5a` (Basic.lean:2458).  cstar.tex:1244-1257 defers these five facts; the solution keyed to the point (asols.tex:1334-1336) reads "Please contact me if you've found a short and elementary proof for any of these five problems", so nothing is printed and no sibling or erratum supplies it.  `0 ≤ a²` for self-adjoint `a` is 5(a) exactly.
- CONFIRMED — acstar-basic.csv:112, 11XV `spectrum_self_adjoint_real_1` (Basic.lean:2966).  cstar.tex:1570-1573 part 1; solution asols.tex:1377-1387 divides by `Im λ` to reduce to 11XIII, and the Lean forms `b := (Im λ)⁻¹(a - Re λ)` and applies `selfAdjoint_sub_I_isUnit`.  No Mathlib `im_eq_zero_of_mem_spectrum` in it.
- CONFIRMED — acstar-basic.csv:126, 11XXI `spectrum_basic_6` (Basic.lean:3542).  cstar.tex:1677-1678 part 6; solution asols.tex:1524-1531 is the factorisation `λ - a = λa(a⁻¹ - λ⁻¹)`, which is the Lean's `hfac`.  `spectrum.map_inv` is not used.

## `acstar-matrices-representation.csv` (10 rows)

- CONFIRMED — acstar-matrices-representation.csv:31, 32XIII `example (CStarAlgebra (Bax 𝒜 X))` (Theses/A/CStar/Matrices.lean:981).  Print cstar.tex:5332 (Proposition); the Lean asserts it over the subalgebra of `X →L[ℂ] X`, so multiplication is composition and `bax_norm_coe`/`bax_star_spec` make the norm and involution the printed ones.  The printed proof (32XIV, cstar.tex:5341) says only closedness remains given 32XI+32XII, and the instance block supplies exactly that.
- CONFIRMED — acstar-matrices-representation.csv:35, 32XIII `bax_isSelfAdjoint_iff` (Matrices.lean:945).  The Proposition's "adjoint as involution" clause read at self-adjoint elements; the full clause is on the named sibling `bax_star_eq` (:938).  32XIII does print an argument, and the two-line proof off `bax_star_spec`/`bax_star_eq` is part of the printed assembly.
- CONFIRMED — acstar-matrices-representation.csv:49, 33I `toCLMBax` (Matrices.lean:1634).  Pure `def` pinning Mathlib's `CStarMatrix.toCLM` to `Bax 𝒜 (𝒜^N)`, the map `A ↦ Ā` of 33I.1 (cstar.tex:5417).  Aside: 33I does have a solution (asols.tex:4043) and the def's one obligation is discharged by `cstar_matrices_1`, which transcribes it — a strict reading would say `faithful`; not filed, because the def carries no proof of its own and every other `def` row in this file is graded the same way.
- CONFIRMED — acstar-matrices-representation.csv:51, 33I `toCLMBax_surjective` (Matrices.lean:1646).  33I.2's surjectivity half (cstar.tex:5427), square case; the general `M×N` bijection is the sibling `cstar_matrices_2` (:1531), whose construction `A_{ij} = T(eᵢ)ⱼ` is verbatim solution `parsec-330.10`(2) (asols.tex:4074-4085).
- CONFIRMED — acstar-matrices-representation.csv:118, 27X `riesz_ideal_basic_1` (Theses/A/CStar/Representation.lean:578).  cstar.tex:4009 part 1, all four clauses stated (riders on the siblings `_1b`/`_1c`); the proof is solution `parsec-270.100`'s own order (asols.tex:2694).
- **DEFECT — acstar-matrices-representation.csv:127, 27XVII `spectrum_eq_range_elemental` (Representation.lean:1824).  `proof` should be `route`.**  27XVII (cstar.tex:4145) prints an argument — solution `parsec-270.170` (asols.tex:2846): `λ ∈ spec(a)` iff `a−λ` non-invertible iff `f(a−λ)=0` for some character, by 27XV `inv-mult-state`.  The Lean is `rw [← StarSubalgebra.spectrum_eq …]; exact spectrum_eq_range_char b`, i.e. Mathlib spectral permanence plus a lemma that reaches the claim through Gelfand's theorem, using 27XV nowhere.  That sibling (`spectrum_eq_range_char`, line 124 of this file) was regraded `route` by the first 2026-09-05 draw for exactly this reason; this row inherits the same route and the same defect.  `stmt` stays `ok`.
- CONFIRMED — acstar-matrices-representation.csv:128, 27XVIII `gelfand_representation_isometry` (Representation.lean:926).  cstar.tex:4149 states `‖γ(a)‖ = ‖a‖`; the Exercise's printed hint ("first assume `a` self-adjoint, use `spectrum-miu` and `norm-spectrum`; for the general case the C*-identity") is what the proof does, deliberately avoiding Mathlib's `gelfandTransform_isometry`.
- CONFIRMED — acstar-matrices-representation.csv:133, 27XXII `sw_two_point` (Representation.lean:1122).  cstar.tex:4187; the Lean abstracts `g(y)` to `t ≥ 0` (at `t=0` the zero function serves, so nothing is lost) and walks the print's replacements in order — `f − f(x)`, sup with `0`, the scalar `t/v(y)`, then `⊓ t`.
- CONFIRMED — acstar-matrices-representation.csv:164, 30IV `omega_norm_basic_2_counterexamples` (Representation.lean:2879).  30IV.2 (cstar.tex:4787) asks to refute four inequalities; the Lean bundles all four over one positive `ω : M ↦ M₀₀` on `M₂(ℂ)`.  Witnesses 1-3 are the exercise's own printed hints.  Aside: for the fourth the Lean uses the matrix unit `e₀₁` where solution `parsec-300.40` (asols.tex:3411) uses `½(⁰⁰₁₁)` — same phenomenon, documented in the doc comment; `mild` would be arguable, `faithful` stands.
- CONFIRMED — acstar-matrices-representation.csv:171, 30X `conjMap` (Representation.lean:3076).  `conjMap b = a ↦ b*ab`, the map inside clause 3 of the Proposition (cstar.tex:4977).  A pure definition with no proof obligation; 30XI-30XIII prove the equivalences, not this map.

## `acstar-positive.csv` (12 rows)

- CONFIRMED — acstar-positive.csv:21, 14III `norm_segIntegral_le` (Theses/A/CStar/Positive.lean:850).  14III is the Definition point (cstar.tex:2095-2177) — no proof, no hint, no `parsec-140.30` key; the Lean is an auxiliary about the defined object, graded like its five siblings.
- CONFIRMED — acstar-positive.csv:31, 14VIII `invint_3` (Positive.lean:1903).  cstar.tex:2346-2366 part 3 term for term; solution asols.tex:1825-1884 reduces to `z₀ = 0`, does the two axis-parallel cases off parts 2/2′ and closes by Goursat on the corner triangle — exactly `segment_inv_integral` → `inv_seg_log` → the horiz/vert/corner/split lemmas.
- CONFIRMED — acstar-positive.csv:37, 15IV `polygon_triangle` (Positive.lean:2755).  cstar.tex:2444-2465 does print an argument ("By partitioning the area between `T` and the `N`-gon … into triangles `T₁,…,T_M` (for which `∫_{T_m}f = 0` by `goursat`)"), so `none` is out; the Lean runs that partition-and-Goursat route, so `route` is out.  The deviation is the documented sign — the print's `wn_T(z₀) = −1` against the Lean's `+1` (doc comment Positive.lean:2730-2754, ERRATA.md:132 OPEN) — plus spelling out the "obvious manner": `mild` stands.
- CONFIRMED — acstar-positive.csv:56, 17V `thesisPos_iff_spectrum` (Positive.lean:4167).  `ThesisPos` is the print's item 4/1, so the Lean is 1↔3 of cstar.tex:2748-2770; solution `parsec-170.50` (asols.tex:1929-1945) proves both directions by `parsec-170.30`, which is what the Lean uses.
- CONFIRMED — acstar-positive.csv:67, 17VI `thesisPos_pow` (Positive.lean:4343).  Item 4's last clause (cstar.tex:2802); solution item 4 (asols.tex:2011) says "All this follows from `parsec-110.150`, using … that self-adjoint `b` is positive iff `spec(b) ⊆ [0,∞)`", and the Lean does exactly that via 11XV.2/11XV.3 through `thesisPos_iff_spectrum`.
- CONFIRMED — acstar-positive.csv:76, 17VI `positive_basic_2_2` (Positive.lean:4943).  Item 2 (cstar.tex:2779), `𝒜₊` closed; solution item 2 (asols.tex:1955-1972) is the sequence argument, and `isClosed_thesisPos` (:4243) is that argument step for step, this row only crossing the `thesisPos_iff_nonneg` bridge.
- CONFIRMED — acstar-positive.csv:89, 20II `weak_russo_dye_1` (Positive.lean:5239).  Statement cstar.tex:2884-2893, proof cstar.tex:2894-2900 as corrected by erratum `parsec-200.30` (asols.tex:86-92); the Lean runs it in order, with self-adjointness of `f a` supplied inline by 10V's printed expression.
- CONFIRMED — acstar-positive.csv:135, 22IV `qSandwiched` (Positive.lean:6841).  The printed `J` is at cstar.tex:3428-3438, inside 22IV's proof; a `private def` with nothing printed to follow.  (Aside: the note calls `qSandwiched` "the order ideal `J`" when `J` is the submodule at :6878 built from it — imprecise wording, both grades right.)
- CONFIRMED — acstar-positive.csv:143, 23II `sqrt_unit_exists` (Positive.lean:7352).  cstar.tex:3593-3597; the Lean applies `sqrt_lemma_exists` to `1 − x` and takes `d := 1 − b`, the printed step.  The `‖a‖ ≤ 1` restriction is the print's own (cstar.tex:3496) and the general case is the sibling `sqrt_exists_core`.
- CONFIRMED — acstar-positive.csv:144, 23II `mul_nonneg_of_commute` (Positive.lean:7432).  cstar.tex:3598-3606's five steps and nothing else, closing on `positive_basic_2_4a`.  (Aside: strictly this is point {40} = 23IV, filed under the enclosing Lemma's DISP as the whole sqrt family is.)
- CONFIRMED — acstar-positive.csv:196, 26II `commutative_cstar_basic_4_inf` (Positive.lean:9250).  Item 4 (cstar.tex:3890), binary-infimum half, the rest on named siblings; the solution closes with "`f(a∧b) = f(a)∧f(b)` since `a∧b = −((−a)∨(−b))`" (asols.tex:2624) and the Lean derives `IsGLB` from the supremum case by negation.
- CONFIRMED — acstar-positive.csv:198, 26II `commutative_cstar_basic_6` (Positive.lean:9314).  cstar.tex:3899-3903 (item 6 in the current source); the print's second sentence about `a∧b`/`a∨b` is on the sibling `meet` (:9351).  The solution's two steps (asols.tex:2645-2659) are the Lean's two steps.

## `acstar-towardsvn-avn-completeness.csv` (9 rows)

- CONFIRMED — acstar-towardsvn-avn-completeness.csv:8, 36III `selfDual_pi` (Theses/A/CStar/TowardsVN.lean:666).  cstar.tex:6134 is a bare Exercise with no hint; `asols.tex` has no `parsec-360.*` key and `ERRATA.md` nothing on 36.  The Lean states `SelfDual 𝒜 (WithCStarModule 𝒜 (Fin N → 𝒜))` against the `SelfDual` of 36I.
- CONFIRMED — acstar-towardsvn-avn-completeness.csv:49, 39IV `orthonormal_1` (TowardsVN.lean:1928).  cstar.tex:6549 part 1 (Bessel); the Lean adds the `Summable` the printed `∑` presupposes and the printed proof of part 2 consumes.  Proof runs the printed chain of 39V.1 (cstar.tex:6577-6588) via `orthonormal_finset_sub_norm_sq`, then the tsum bound.
- CONFIRMED — acstar-towardsvn-avn-completeness.csv:59, 39VII `ketbra_sum_nonneg` (TowardsVN.lean:2161).  A `private` auxiliary (finite `∑|e⟩⟨e| ≥ 0`) serving the printed proof at cstar.tex:6643/6654; the point is stated and graded on the sibling `bh_np_lemma`, and the auxiliary's own claim has no printed counterpart.
- CONFIRMED — acstar-towardsvn-avn-completeness.csv:65, 39VI `sum_ketbras_3` (TowardsVN.lean:2250).  cstar.tex:6622 part 3, `ω(1)=∑_{e∈ℰ}ω(|e⟩⟨e|)`, as `HasSum`.  No hint, no `parsec-390.60` solution or erratum.  Aside: part 3's "Conclude" points at parts 1-2 and is the route the Lean takes, but "conclude that P" is the exercise's phrasing, not a printed hint.
- CONFIRMED — acstar-towardsvn-avn-completeness.csv:89, 72V `normal_functionals_lemma_four_not_equivalent` (Theses/A/VN/Completeness.lean:842).  Ours, not the thesis's: the `H = ℂ²` witness for `ERRATA.md` 72V.4, showing clause (2) of vn.tex:3886 holds while clause (4) fails.  Nothing printed to match; the point itself is carried, with its repair, by `normal_functionals_lemma`.
- CONFIRMED — acstar-towardsvn-avn-completeness.csv:125, 75VIII `relCoceil` (Completeness.lean:3468).  A `noncomputable def` rendering the `⌈ω⌉_𝒮` that vn.tex:4590 introduces only inside the proof of `vnsac` and never states.  The complemented rendering is the one `ERRATA.md` 75IX endorses by name; the point is stated by the sibling `vnsac`.
- CONFIRMED — acstar-towardsvn-avn-completeness.csv:147, 77V `continuous_ultraweak_of_npFunctional` (Completeness.lean:4370).  Auxiliary: `g` ultraweakly continuous once `ω ∘ g` is, proved by unfolding `ultraweak B` as `⨅ ω, induced …`.  Not a printed statement anywhere in parsec 770; 77V itself is stated by `vn_extension`.
- CONFIRMED — acstar-towardsvn-avn-completeness.csv:150, 77V `uw_map_of_cont` (Completeness.lean:4413).  Auxiliary: a topological restatement of `Continuous.tendsto` through the induced topology, no printed counterpart.
- CONFIRMED — acstar-towardsvn-avn-completeness.csv:152, 77V `vn_extension_norm, vn_extension_norm_le, vn_extension_norm_eq` (Completeness.lean:4694, :4750, :4763).  The "moreover" of vn.tex:4884-4885 in full; the hard half's proof is the printed one at vn.tex:4929-4941 (the 74VI net with `‖s_α‖ ≤ (1+ε)‖a‖`, the bound on `f(s_α)`, passage to the ultraweak limit, then `ε` arbitrary).

## `aproc-duplicators-quantumlambda.csv` (13 rows)

- CONFIRMED — aproc-duplicators-quantumlambda.csv:32, 129VIII `continuous_measure_space` (Theses/A/Proc/Duplicators.lean:1309).  proc.tex:6311 exactly; the proof is the printed one at point {90} (halving to get arbitrarily small positive-measure subsets, then `measure_zorn`), run relatively and instantiated at `univ`.  `ERRATA.md`:82's printed `X∖A`/`A₁∖A` slip is a typo transcribed correctly, not a route change.
- CONFIRMED — aproc-duplicators-quantumlambda.csv:45, 130IV `star_mul_apply` (Duplicators.lean:2734).  130IV (proc.tex:6524) prints only "Show that `L^∞(X) ≅ ⊕_{A∈𝒜} L^∞(A)`" with no hint, and no sibling point of parsec 130 argues it ({30} proves {20}, {50} is an unproved Corollary).  The pointwise `(u*u)(x) = |u(x)|²` contradicts nothing printed.
- **DEFECT — aproc-duplicators-quantumlambda.csv:53, 132III `exists_nmiu_mul` (Duplicators.lean:4219).  `proof` should be `faithful`.**  132III.2 (proc.tex:6683) prints a hint — "**Deduce** from this and `\sref{duplicable}`" — and this declaration is precisely that deduction: `duplicable_forward hd` gives `𝒜 ≅ ℓ^∞(X)` and `linf_nmiu_mul` is transported back along it.  The row's premise ("the thesis does not isolate this step, so there is no printed argument") is the pre-2026-09-05 reading; the same point's part-2 row `dup_vna_is_monoid_2` is already graded `faithful` off that very hint.  `stmt` is right.
- CONFIRMED — aproc-duplicators-quantumlambda.csv:54, 132III `dup_vna_is_monoid_2` (Duplicators.lean:4296).  The four conjuncts are part 2's two iffs, the `ℓ^∞(X)` classification and `m(a⊗b) = a·b`; "commutative" is on the named sibling `dup_vna_is_monoid_4`.  The proof runs the printed hint (part 1 plus 127III plus 128VIII).
- CONFIRMED — aproc-duplicators-quantumlambda.csv:80, 122II `linfNspAdjunction` (Theses/A/Proc/QuantumLambda.lean:753).  122II's second sentence (proc.tex:4530-4542); built by `Adjunction.mkOfHomEquiv` over `linfNspHomEquiv`, which is `first_adjunction`'s `∃!` — the thesis's own "a known and easily checked fact" — with the print's formula on the sibling `linfMap_apply`.
- CONFIRMED — aproc-duplicators-quantumlambda.csv:82, 122IV `lpSumSA_mono` (QuantumLambda.lean:158).  122IV's printed proof (proc.tex:4597) argues nothing about monotone finite partial sums; the lemma exists only to feed `DirectedOn` to the Lean formulation of normality.
- CONFIRMED — aproc-duplicators-quantumlambda.csv:86, 122IV `nmiu_functional_product` (QuantumLambda.lean:520).  The Lemma at proc.tex:4591 verbatim; the proof is the single term `lp_nmiu_functional_factors`, whose own proof runs the printed argument step for step (at most one `i` with `φ(e_i) ≠ 0`, `φ(e_i^⊥) = 0`, `φ(a) = φ(e_i a)`, `φ' := φ ∘ κ_i`).
- **DEFECT — aproc-duplicators-quantumlambda.csv:110, 125II `card_le_of_denseRange` (QuantumLambda.lean:1536).  `proof` should be `faithful`.**  125II prints a full proof (proc.tex:4825) and this declaration renders one of its steps — "since every element of `ℋ_ω` is the limit of a sequence of elements from `𝒜`, `#ℋ_ω ≤ #𝒜^ℵ₀`" (exponents per `ERRATA.md`:79(a)) — by exactly that argument: pick a sequence from the dense range per point, injective by uniqueness of limits.  Its own doc comment says so, and the direct analogue for 124I, `card_le_of_dense` in the same file, is graded `faithful` in this very CSV.  `stmt` stands.
- CONFIRMED — aproc-duplicators-quantumlambda.csv:112, 125II `card_npFunctional_le` (QuantumLambda.lean:1624).  proc.tex:4826's chain `#Ω ≤ #ℂ^{#𝒜} = (2^{ℵ₀})^{#𝒜} = 2^{#𝒜}` run exactly (`mk_arrow`, `mk_complex`, `power_mul`, `aleph0_mul_eq`), with the print's "𝒜 is infinite" as the hypothesis `ℵ₀ ≤ #𝒳`.
- CONFIRMED — aproc-duplicators-quantumlambda.csv:115, 125IV `equaliser_lemma` (QuantumLambda.lean:6906).  All three clauses of Kornell's Lemma (proc.tex:4852); `≤` against the print's "less than" is `ERRATA.md`:80 and is what the printed proof gives.  The delegate `equaliser_lemma_of_intersectionTensorStatement` runs the printed proof (`vn_gns_bound`, the slices `r_ξ`, `𝒜̃ := wstar(range r)`, the count, naturality, and `h(𝒟) ⊆ 𝒜̃⊗𝒞`).
- CONFIRMED — aproc-duplicators-quantumlambda.csv:122, 125VIII `FreeExp` (QuantumLambda.lean:2233).  A `structure` giving the universal arrow with `∃!` factorisation, matching 125VIII's "`(·)⊗𝒜` has a left adjoint" (proc.tex:5054) on the side that point uses.  A pure definition; the Theorem's own proof is carried by `tensor_closed`.
- CONFIRMED — aproc-duplicators-quantumlambda.csv:128, 125cII `GeneratesMat` (QuantumLambda.lean:2706).  125cII (proc.tex:5290) is a notation point with no argument; `wstar (MatAlg n) (range f) = ⊤` is its `W*(f(𝒜)) = M_n` on the nose.
- CONFIRMED — aproc-duplicators-quantumlambda.csv:138, 125eVI `TensorBEquiv` (QuantumLambda.lean:8164).  Point {60} at proc.tex:5636 is a Definition; the Lean is the printed `(·)⊗ℬ`-equivalence exactly.  The unrendered parenthetical "(which implies `N_{f₁} = N_{f₂}`)" is a derivable aside, not a clause.

## `aproc-measurement.csv` (12 rows)

- CONFIRMED — aproc-measurement.csv:6, 94II `instance StarOrderedRing (Corner A e)` (Theses/A/Proc/Measurement.lean:661).  proc.tex:194 part 5 asks that `e𝒜e` with the operations of `𝒜` and unit `e` is a C*-algebra; the instance asserts the lifted Loewner order is the star-order, which is what the `CStarAlgebra` bundle at :604 needs.  Part 5 prints no argument and no hint (only part 4 carries a `(Hint: …)`), and parsec 94 has no solution or erratum.
- CONFIRMED — aproc-measurement.csv:18, 94II `corner_vna_basic_10'` (Measurement.lean:1282).  proc.tex:238-245 part 10's two topology clauses, both stated.  Borderline: part 10 says "**Deduce from this** that the ultraweak topology …", which under a strict reading is a printed route, and the Lean proof does exactly that; `none` is kept because the thesis marks its hints explicitly with `(Hint: …)` and the file is consistent — part 4, the only `(Hint: …)`, is the only 94II row graded `faithful`, and part 8's "Deduce from this"/"Conclude" rows are `none`.
- CONFIRMED — aproc-measurement.csv:22, 95I `IsCornerMap` (Measurement.lean:1630).  proc.tex:263-283, the Definition plus the closing "unital corner" convention; a `def` under a Definition point with no proof and no hint.
- CONFIRMED — aproc-measurement.csv:38, 98II `filter_basic_2` (Measurement.lean:2517).  proc.tex:589-595 part 2, all three clauses.  The part prints a hint ("by proving first that `c_p` is injective using `mult-cancellation`") and the Lean follows it: `filter_basic_1` gives `c = c_p ∘ α`, and `stdFilter_injective` is `c_p` injective via `ad_injective` = 60VIII.
- CONFIRMED — aproc-measurement.csv:48, 98VII `filter_corner_formula` (Measurement.lean:2864).  proc.tex:642's closing clause `g(a)=√p \ f(a) / √p`, justified in the printed proof at proc.tex:691 by "(by the proof of `canonical-filter`)"; the Lean derives precisely that via `ldiv_div_ad`.
- CONFIRMED — aproc-measurement.csv:87, 101VI `contraposed_iff_diamond` (Measurement.lean:4749).  proc.tex:1091's second iff, with the sibling `contraposed_iff` carrying both.  Point {60} prints no proof and no hint, and parsec 101 has no solutions.
- CONFIRMED — aproc-measurement.csv:110, 103II `purely_positive_examples_2` (Measurement.lean:5792).  proc.tex:1412 Examples part 2 exactly; an Examples point with no printed argument.
- CONFIRMED — aproc-measurement.csv:113, 103III `purely_positive_basic_3` (Measurement.lean:5826).  proc.tex:1425 part 3 matched exactly; only part 2 carries a cross-reference, part 3 prints nothing.
- CONFIRMED — aproc-measurement.csv:115, 104III `centrally_similar_basic_1` (Measurement.lean:5843).  proc.tex:1465 part 1, both clauses.  Part 1 prints no hint (only part 5 does) and the `parsec-1040.30` erratum (asols.tex:430) touches only points 2a-5.
- CONFIRMED — aproc-measurement.csv:137, 104VII `isLUB_spectralProj_ceil` (Measurement.lean:7722).  An auxiliary about `spectralProj q t = ⌈(q−t)⁺⌉`, corresponding to no printed clause.  Borderline: 104VII does have a printed proof at the sibling point 104VIII (proc.tex:1571ff.), but that proof merely *asserts* the existence of an increasing family and offers a different example with no argument, so there is nothing here for this lemma to follow; the deviation in the choice of family is already charged at row :142 (`mild`), and grading this row `route` would double-count it.
- CONFIRMED — aproc-measurement.csv:144, 105II `exists_chevron` (Measurement.lean:8669).  proc.tex:1696 defines `⟨f⟩` as *the unique* ncp-map making the square commute; this row gives existence with the printed formula and the sibling `chevron_unique` carries the square plus uniqueness.  A Definition point, nothing printed for well-definedness.
- CONFIRMED — aproc-measurement.csv:148, 105III `chevron_f_basic_3` (Measurement.lean:8750).  proc.tex:1723 part 3, both clauses.  Only part 1 of that Exercise carries a hint; part 3 prints nothing.

## `aproc-tensor.csv` (15 rows)

- CONFIRMED — aproc-tensor.csv:19, 110III `IsHilbertTensorProduct.gram_sum_re` (Theses/A/Proc/Tensor.lean:608).  The proof of 110III opens (proc.tex:2255-2263) with `‖∑ᵢγ(xᵢ,yᵢ)‖² = ∑ᵢⱼ⟨γ(xᵢ,yᵢ),γ(xⱼ,yⱼ)⟩ = ∑ᵢⱼ⟨xᵢ,xⱼ⟩⟨yᵢ,yⱼ⟩`; the Lean states that identity and proves it by `sum_inner`/`inner_sum` plus `hγ.inner_mul`, the same two steps.
- CONFIRMED — aproc-tensor.csv:24, 111V `gram_quad_eq` (Tensor.lean:1038).  The quadratic form of a Gram matrix appears nowhere in 111V's printed proof (proc.tex:2457-2495) nor in 111III's Schur proof, which uses only the positivity criterion; it is Lean-only scaffolding for `gram_posSemidef`/`gram_op_le_posSemidef`.
- CONFIRMED — aproc-tensor.csv:26, 111V `gram_op_le_posSemidef` (Tensor.lean:1063).  proc.tex:2486-2492 names exactly `(⟨Axᵢ,Axⱼ⟩) ≤ (‖A‖²⟨xᵢ,xⱼ⟩)` as the justification of the display's last step, and the Lean statement is that matrix inequality.  (The print asserts the inequality rather than arguing it, so `none` would have been arguable; the grade is not wrong.)
- CONFIRMED — aproc-tensor.csv:35, 111XII `vnTensorProduct_nonempty` (Tensor.lean:5799).  111XII (proc.tex:2589-2596) is an Exercise whose hint is "construct a tensor product … using `\sref{ngns}` and `\sref{special-tensor}`"; the Lean obtains `ngns_ulift` for each factor and applies `special_tensor` to the two represented subalgebras — the hint's two ingredients in its order.
- CONFIRMED — aproc-tensor.csv:68, 112X `npFunctional_eq_zero_of_one_eq_zero` (Tensor.lean:3831).  112X's five-part hint (proc.tex:2874-2980) never mentions the degenerate members of `Ω`; the `χ(1)=0 ⇒ χ=0` lemma is Lean's own device for restricting to `Ω₁`.
- **DEFECT — aproc-tensor.csv:71, 112X `orderSeparating_unital_norm` (Tensor.lean:3880).  `proof` should be `faithful`.**  112X.2 prints exactly this step at proc.tex:2911-2919 — "Show that the subset `Ω₁` of `Ω` of unital maps is order separating, and so determines the norm on `𝒯` via `‖a‖²=‖a*a‖=sup_{ω∈Ω₁}ω(a*a)` (see `\sref{order-separating-norm}`)" — and the Lean does precisely that: restrict to the unital members, show they are still order separating (rescaling; degenerate ones vanish), then read the norm off via `order_separating_norm` (21VII), the printed citation.  The file's own scheme grades such printed-step auxiliaries `faithful` (rows :66, :96).  `mild` is the only alternative, since the print does not say *how* `Ω₁` is order separating; `none` is not.  `stmt` stands.
- CONFIRMED — aproc-tensor.csv:82, 113III `matBilinL` (Tensor.lean:4914).  113III (proc.tex:3024-3033) is a Notation point defining `(M_Nβ)(A,B) = (β(A_ij,B_ij))_ij`; `matBilinL` is that map bundled, entry for entry.  The only divergence is C*-algebra binders where the print says von Neumann — harmless generality for a definition.
- **DEFECT — aproc-tensor.csv:98, 115II `exists_npExtension_conjProdNP` (Tensor.lean:6606).  `proof` should be `faithful`.**  This declaration is the printed `ω'` of 115II's proof, and proc.tex:3208-3218 supplies both halves with their reasons: `ω∘β_⊙` extends "to an ultraweakly continuous functional `ω' := (ω∘β)_⊗`" on `𝒜⊗ℬ` (`ERRATA.md` 115II(d)), and "this extension `ω'` is completely positive (because `β` and thus `ω∘β` are completely positive, see `\sref{cp-bilinear}`)" — which is exactly what the Lean proves, positivity from `BilinCP β` via `lift_tmapBilin_star_mul_self_nonneg` and normality from the continuity already in hand.  `stmt` stands.
- CONFIRMED — aproc-tensor.csv:103, 115IV `tensor_functor_id` (Tensor.lean:7010).  115IV (proc.tex:3281-3292) is a bare Exercise with no hint, no solution and no erratum, so `none` is right whatever the Lean does.  `stmt` holds by the sibling convention: this identity law plus `tensor_functor_comp` (:104) plus 115II parts 1-3 give the four classes, and the row's note discloses that the categorical statement itself is not formed.
- CONFIRMED — aproc-tensor.csv:108, 115V `isTensorProduct_genGamma` (Tensor.lean:7089).  The printed proof (proc.tex:3299-3400) checks 108II's three conditions for `γ(a,b)=f(a)⊗g(b)` into the generated `𝒯`, and the Lean runs them in that order with the printed citations (75VII+89XI.2; 89XII along `f`,`g`; the identity `γ(σ̃,τ̃)(t)=(σ⊗τ)(t)`).  The one divergence — the print tests condition 3 at a central projection, the Lean at every positive `t` — is the printed narrowing on file as `ERRATA.md`:70's 115V row, ruled harmless, and a point corrected in the errata is audited against the corrected form.

- CONFIRMED — aproc-tensor.csv:118, 116III.4 `tensor_simple_facts_4_counterexample` (Theses/A/Proc/Tensor.lean:7665).  The print (proc.tex:3433, item 4 at :3459) asks to *show* `⊗` is jointly ultraweakly continuous, with a hint at :3462-3477; the Lean states the exact negation at `𝒜 = ℬ = ℓ^∞(ℕ)`, so it is a refutation.  The transcription with the printed hint is the separate row :116 (`sorry`), and `ERRATA.md` carries 116III.4.  Both grades stand under the file's non-transcription convention (identical twin at :137, `sum_generation_1_is_false|ok|none`), where `ok` reads "correctly stated, no misrepresentation of the print" and nothing printed argues a counterexample.  Convention note for the parent, not a per-row defect: under the brief's literal `stmt` rule this row and its twin would both be `differs` — one convention decision covering two rows.
- CONFIRMED — aproc-tensor.csv:138, 118II `vtmul_mono_left` (Tensor.lean:10142).  A `private` auxiliary used only to build the np-map that 118II's printed proof asserts without argument (proc.tex:3819-3823); nothing is printed for it on either attribution.
- CONFIRMED — aproc-tensor.csv:146, 118IV `ncpCarrier_eq_projSup` (Tensor.lean:10644).  Step 5 of `carrier-tensor` (proc.tex:3943-3962) is carried by `carrier_tensor_5` (:10744, `faithful`); this declaration is the *one-factor* identity `⌈h⌉ = ⋁_ν ⌈ν∘h⌉` for a general ncp-map, the final identification the print glosses over.  (Its docstring's "This is the printed chain" overstates.)
- CONFIRMED — aproc-tensor.csv:160, 119IV `exists_associator` (Tensor.lean:11503).  The Corollary at proc.tex:4037-4051 in full (nmiu, the elementary-tensor equation, bijectivity, uniqueness).  Point {40} is followed by the remark {41}, not a proof point, and parsec 119 has no solution or erratum; the "Corollary" heading implicitly delegates to the trilinear 119II while the Lean goes the bilinear route, but since no argument text is printed, `none` (not `route`) is right.
- CONFIRMED — aproc-tensor.csv:168, 119V `vn_smc_pentagon` (Tensor.lean:12058).  The printed pentagon (proc.tex:4142, diagram :4147-4168) matched arrow for arrow; the proof is `vnt_linear_ext₃` — both routes agree on `a⊗(b⊗(c⊗d))` and the span is ultraweakly dense — which is literally the printed one-sentence argument at :4169-4174.

## `avn-basic.csv` (10 rows)

- CONFIRMED — avn-basic.csv:16, 42III `continuous_ultraweak_of_forall` (Theses/A/VN/Basic.lean:3051).  42III (vn.tex:211, point {30}) defines the ultraweak topology as "the least topology that makes all normal positive linear maps continuous"; this declaration is the minimality half of that (all `ω ∘ f` continuous ⟹ `f` continuous), the definition itself being on the sibling rows `ultraweak`/`ultrastrong` and the net characterisations on `uwTendsto_iff`/`usTendsto_iff`.  A definitional point with no printed argument — the "One can verify" sentences are unproved assertions — so `none` is right.
- CONFIRMED — avn-basic.csv:27, 43I `continuous_ultrastrong_npFunctional` (Basic.lean:563).  43I (`uwweaker`, vn.tex:341, point {10}) is a bare three-part Exercise with no hint on any part; its three parts are carried by `norm_apply_le_omegaNorm` (:26) and `uwweaker_1/2/3` (:29-31).  This is a derived helper (`ω` ultrastrongly continuous) whose own claim is printed nowhere, so `none` is right.
- **DEFECT — avn-basic.csv:43, 43II `vn_counterexamples_5` (Basic.lean:1549).  `proof` should be `faithful` (`mild` at the outside); `none` is wrong.**  43II part 5 (vn.tex:374, `vn-counterexamples`) prints a route, not just a claim: "Show that the unit ball `(ℬ(ℓ²))₁` … is not ultrastrongly compact **by proving that `(|0⟩⟨n|)ₙ` has no ultrastrongly convergent subnet**."  That printed route is itself defective and is corrected in our own `ERRATA.md`:37 (read `(|n⟩⟨0|)ₙ`), whose repair column ends "Our `vn_counterexamples_5` … **silently carries the repair**: it uses `ketbraNat n 0`, and derives the contradiction from compactness of the image of the ball under the ultrastrongly continuous `a ↦ a e₀`".  So an argument *is* printed (and repaired by an erratum on file) and the Lean follows it; under the 2026-09-05 convention no 43II.5 row can be `none`.  `stmt` is right — the Lean states exactly "the closed unit ball is not ultrastrongly compact".
- CONFIRMED — avn-basic.csv:52, 44III `vanishing_effects` (Basic.lean:1920).  The Lemma at vn.tex:654 (point {30}) verbatim, and the proof at vn.tex:663-685 transcribed: `|ω(x_αb_α)|² = |ω(√x_α·√x_α b_α)|² ≤ ω(x_α)ω(b_α*x_αb_α) ≤ ω(x_α)ω(b_α*b_α) ≤ ω(x_α)ω(1)`, with Kadison's inequality entering as `norm_apply_star_mul_le` (the `omega-norm-basic` the print cites), then the squeeze.
- CONFIRMED — avn-basic.csv:62, 44VIII `conjSA_isLUB` (Basic.lean:2455).  A `private` restatement of 44VIII on the `selfAdjoint A` subtype, proved by transport from `ad_normal` (row :61), which is where 44VIII's printed proof (vn.tex:714-756, points {90}/{100}) is graded `faithful`.  The transport contains none of the printed argument, so `none` is right — the grade this file gives every other non-printed auxiliary of the parsec, and the reading the first 2026-09-05 draw endorsed by name for this row.
- CONFIRMED — avn-basic.csv:73, 44XIII `vna_supremum_commutes` (Basic.lean:2818).  Point {130} `vna-supremum-commutes` (vn.tex:787) verbatim — `a` commutes with every `d ∈ D` ⟹ `a` commutes with `⋁D`.  A bare "Show that" with no hint: the enclosing exercise {120} carries the hint only on its *other* sub-point {140} ("Use `vanishing-effects` to show that …"), and this sub-point has none.  The Lean's route (44VII plus 44XI separation) is unprinted.
- CONFIRMED — avn-basic.csv:74, 44XIII `vna_infimum_commutes` (Basic.lean:2847).  The downward-directed dual, obtained from `vna_supremum_commutes` by `x ↦ −x` and labelled as such in its own doc comment ("in the 'variation' used in the proof of 56VI.80").  Nothing printed for it; `ok`/`none` is the grading this file already gives the parallel variation `ad_normal_inf` (:63).
- CONFIRMED — avn-basic.csv:76, 44XIV `le_norm_smul_one` (Basic.lean:140).  A public auxiliary (`0 ≤ a ⟹ a ≤ ‖a‖ • 1`) whose doc comment states its own filing: "cstar.tex 24-ish; stated here because it is needed for **44XIV**".  It is not 44XIV's statement — that is on the sibling `vna_supremum_uslimit` (:75) — and its own claim has no printed counterpart in parsec 440, so `none` is right; the DISP is a filing locator, and the first 2026-09-05 draw likewise listed this row among the correctly-`none` auxiliaries of the parsec.
- **DEFECT — avn-basic.csv:88, 46III `preservesDirSups_of_continuous_ultrastrong` (Basic.lean:3903).  `proof` should be `route`; `none` is wrong.**  46III (`npuws`, vn.tex:940, point {30}) is an Exercise that prints a hint: "(Hint: combine `\sref{p-uwcont}` and `\sref{cp-uscont}`.)" — i.e. 44XV and 45II.  This declaration is the substantive `(3) ⇒ (1)` implication of the printed TFAE (its doc comment says so), and its proof uses neither: it runs `vna_supremum_uslimit` (44XIV) directly, taking the net `(d)_{d∈D}` ultrastrongly to `⋁D` and comparing real and imaginary parts.  Nor does the shell `npuws` (:89, three `tfae_have`s) touch 44XV or 45II — `1→2` is definitional, `2→3` is `ultrastrong_le_ultraweak`.  So a hint is printed and is not followed: `route`.  (Reading the row instead against 45I — whose own hint, "use that a bounded directed set `D ⊆ sa(𝒜)` converges ultrastrongly to `⋁D`", is exactly what the proof does, and which the doc comment invokes as "**45I**.1 for `B = ℂ`" — would give `faithful`.  Either way `none` is wrong.  This is the same shape the first 2026-09-05 draw filed against `us_cont_normal` at :81.)
- CONFIRMED — avn-basic.csv:144, 54IV `approx_closure` (Basic.lean:7893).  The Lemma at vn.tex:1949 (point {40}), the printed chain of iffs rendered as two.  The proof at vn.tex:1958-1972 transcribed: `U ≈ closure U` by `meagre-basic`, then `W \ closure W'` is an open subset of the meagre symmetric difference, hence empty by Baire (`baire_category_theorem`), hence `closure W ⊆ closure W'`, and symmetrically.

## `avn-division-normalfunctionals.csv` (18 rows)

- CONFIRMED — avn-division-normalfunctionals.csv:3, 79I `isPseudoinverse_unique` (Theses/A/VN/Division.lean:57).  The Definition `dfn-pseudoinverse` (vn.tex:5089-5107) states uniqueness at vn.tex:5099-5100 with nothing but the parenthetical "(by `\sref{mult-cancellation}`)", and under the 2026-09-04 ruling recorded in `SAMPLE-2026-09-04-findings/NOARG-deferred.md`:97 a bare parenthetical citation is not a printed argument.  *Lead outside the sample:* line :53 of this CSV (81I `exists_div`) grades the identical parenthetical from 810.10 as `faithful`; one of the two is mis-graded, and it is not this one.
- CONFIRMED — avn-division-normalfunctionals.csv:15, 79II `pseudoinverse_equivalents` (Division.lean:136).  All six equivalents of the Lemma at vn.tex:5108-5130 in the printed order and shape; the proof at vn.tex:5132-5175 is what the Lean does — (5⇔6) by permuting the four conjuncts, then L1=(1)⇒(2), L2=(2)⇒(5), L3=(5)⇒(1) instantiated at `(a,t)` and at `(t,a)` (the print's "follows along the same lines"), with `mult_cancellation` at each printed step.
- CONFIRMED — avn-division-normalfunctionals.csv:17, 79V `pseudoinverse_basic_1` (Division.lean:283).  790.50 item 1 (vn.tex:5185-5189) exactly; the Exercise prints no hint on any of its three items, no sibling point of parsec 790 argues it, and there is no solution past parsec 34.
- CONFIRMED — avn-division-normalfunctionals.csv:30, 79VI `pseudoinverse_basic_2'_4_forces_eq_one` (Division.lean:753).  An auxiliary instantiating the *pre-erratum* 790.60.4 conclusion at `b = p` a projection, `c = 1`.  790.60 (vn.tex:5202-5236) prints no hint on any of its five items, and erratum `parsec-790.60` (asols.tex:380-385) supplies a corrected statement, not an argument.
- CONFIRMED — avn-division-normalfunctionals.csv:32, 79VI `pseudoinverse_basic_2'_4_is_false` (Division.lean:799).  Refutes the pre-erratum conclusion in `ℓ^∞(Fin 2)`; hypothesis set is the printed one.  Same `none` reasoning.  One staleness, note-level only: the doc comment cites "vn.tex:5222" for `c^{∼1} ≤ b^{∼1}`, but that line now carries the *corrected* form — the wrong form survives only in the printed book and asols.tex:381-384.
- CONFIRMED — avn-division-normalfunctionals.csv:68, 81III `proto_douglas_1` (Division.lean:2116).  The Lemma at vn.tex:5395-5404, its uniformity clause on the named sibling `proto_douglas_2`; the proof follows vn.tex:5405-5460 step for step (the block estimate, ultrastrong Cauchy from 77I completeness, `⌈a⌋ ≤ ⌈b⌋` giving `a = cb`, and `tₙ⌊b⌉ = tₙ` giving `c⌊b⌉ = c`).
- CONFIRMED — avn-division-normalfunctionals.csv:72, 81V `div_smul_left` (Division.lean:2366).  A genuine auxiliary — `(z•a)/b = z•(a/b)` appears nowhere in parsec 810; it is scaffolding for the λ-rescaling in `douglas_1` and `div_uwc_ball_of_norm_le`.
- CONFIRMED — avn-division-normalfunctionals.csv:73, 81V `douglas_1` (Division.lean:2373).  810.50 item 1 (vn.tex:5463-5473) verbatim under `0 ≤ l`.  The only parenthetical is "(Compare this with ``Douglas' Lemma'' from `\cite{douglas}`)" — a pointer to the literature, not a hint — and 810.60's hint is for a different point.
- CONFIRMED — avn-division-normalfunctionals.csv:85, 81IX `div_usc_ball` (Division.lean:2974).  The ultrastrong half of the Lemma at vn.tex:5533-5545, the ultraweak half being the sibling `div_uwc_ball`/`div_uwc`; it is the clause erratum `parsec-810.90` (asols.tex:389-401) leaves standing, the erratum touching only the second map.  The proof is the printed one at vn.tex:5546-5560 (`douglas_1` at λ=1, `proto_douglas_2`'s uniform convergence, each partial sum ultrastrongly continuous by 44VIII, then the ε/3 uniform-limit step).
- CONFIRMED — avn-division-normalfunctionals.csv:102, 83V `cceil_sum` (Division.lean:3588).  830.50 (vn.tex:5733) with its proof at 830.60; the Lean's explicit `Pairwise` orthogonality is what the printed `∑` already means.  The proof runs the printed steps in order (Zorn maximal orthogonal family, `eᵢ = uᵢ*uᵢ ≤ ⌈uᵢ*euᵢ⌉ ≤ ⌈⌈e⌉⌉`, `p = ⋃ₐ⌈(eap)*eap⌉`, polar decomposition, maximality contradiction), reading `ERRATA.md`:50's `u*u` for the printed `e`.
- CONFIRMED — avn-division-normalfunctionals.csv:103, 84II `cstar_jacobson_eq_bot` (Division.lean:3767).  840.20 (vn.tex:5783) does print a proof, but that proof is the norm-compactness route and is graded on the sibling row :108 `fdcstar` (`route`); this is a `private` auxiliary feeding the Wedderburn–Artin detour and containing none of the printed argument.  *(Later the same day: the detour's five auxiliaries had no consumer left once 84II ran the printed proof, and were deleted with their rows — `docs/DEAD-LIMBS.md` §16.  This row and the four beside it are gone; `fdcstar`, cited above as :108, is now :112.)*
- CONFIRMED — avn-division-normalfunctionals.csv:117, 84aI `effectPUMap_isUnitalMap` (Division.lean:4468).  841.10 (vn.tex:6034)'s printed argument is transcribed on the sibling `cstar_no_pu_equalisers_example` (:127, `faithful`); this is a `private` one-line fact (`p_a(1,1) = a + (1-a) = 1`) that the Example only presupposes.
- CONFIRMED — avn-division-normalfunctionals.csv:139, 84bIII `starAlgHom_mono₂` (Division.lean:5538).  842.30 (vn.tex:6184)'s proof at 842.40 is transcribed on the sibling `hereditarilyAtomic_subalgebra` (:143); this is a `private` universe-polymorphic restatement proved generically from `starAlgHom_nonneg₂`.
- CONFIRMED — avn-division-normalfunctionals.csv:159, 86XII `uwcont_on_ball` (Theses/A/VN/NormalFunctionals.lean:790).  860.120 (vn.tex:6462) with proof at 860.130, matched clause for clause; the proof is the printed one (86IX polar decomposition, normality of `f(u(·))` from `mult_uws_cont` plus continuity on the effects, then `f = g ∘ (u*·)`).
- CONFIRMED — avn-division-normalfunctionals.csv:163, 87VI `norm_predual` (NormalFunctionals.lean:939).  870.60 (vn.tex:6590) with proof at 870.70, followed in order; order-separation of the np-functionals stands in for the printed `‖·‖ = sup_ω |ω(·)|`, which is exactly what the print's parenthetical "(which is order separating)" licenses.  The only addition is the Cauchy–Schwarz estimate for `‖f‖ ≤ 1`, which the thesis asserts — a supplied gap, not another route.
- CONFIRMED — avn-division-normalfunctionals.csv:178, 88V `amp_apply` (NormalFunctionals.lean:1604).  880.50 (vn.tex:6764)'s items are carried by the sibling rows :186/:187/:190; `amp_apply` is the `rfl` defining equation of `amp`, pure infrastructure.
- CONFIRMED — avn-division-normalfunctionals.csv:188, 65III `star_mem_commutant_of_starSubalgebra` (NormalFunctionals.lean:1726).  650.30 (vn.tex:3221) item 3 prints a hint only for the *counterexample* clause ("compute `{(0 1;0 0)}^□` in `M₂`"); the clause this row renders — `S` star-closed ⟹ `S^□` star-closed — carries no argument and no hint.  (The counterexample clause is row :121 of `avn-projections.csv`, and it *is* defective — see below.)
- CONFIRMED — avn-division-normalfunctionals.csv:205, 89IX `exists_sumVectorNP` (NormalFunctionals.lean:3462).  890.90 (vn.tex:7116) prints a proof at 890.100, transcribed on the sibling `normal_functional` (:207); this declaration is the *converse* direction, which 890.90 does not state, proved from 38IV.2 plus countable-support re-indexing.  The doc comment declares the divergence.

## `avn-projections.csv` (22 rows)

- CONFIRMED — avn-projections.csv:7, 55III `projection_examples_3` (Theses/A/VN/Projections.lean:273).  vn.tex:2209 point {30} is an "Examples" point and item 3 (vn.tex:2220-2227) prints no proof and no hint; no sibling point of parsec 550 argues it.  The Lean iff carries both halves of item 3 including the print's "closed".
- CONFIRMED — avn-projections.csv:26, 55XIV `projection_below_projection` (Projections.lean:650).  vn.tex:2352-2356 is a bare Exercise whose only parenthetical is "(either directly, or using `\sref{orthogonal-tuple-of-projections}`)"; the Lean one-liner `(hp.le_iff_sub hq).mp hpq` is the "directly" branch the print itself sanctions, and that branch prints no argument.  `route` is certainly wrong — nothing printed is bypassed — and the only reading under which this is not `none` treats the bare alternative as a hint, under which the Lean still takes an offered route.
- CONFIRMED — avn-projections.csv:28, 56I `exists_ceil` (Projections.lean:862).  56I (vn.tex:2361) prints a full proof at points {20}-{50}; the Lean reduces `b = 0`, normalises by `‖b‖⁻¹` (the 59I extension, vn.tex:2683) and calls `exists_ceil_effect` (:726), which is the thesis's own `⋁ₙ b^{1/2ⁿ}` construction.  The effect→positive widening and the 59III.1 form are both printed content (59I defines `⌈b⌉` for positive `b`; 59III.1's parenthetical at vn.tex:2733 states this characterisation for positive `a`).  Bookkeeping aside: the DISP would be more accurate as `56I,59I`, as the neighbouring `ceil` row (:29) already is.
- CONFIRMED — avn-projections.csv:49, 56XIII.3 `ceil_floor_basic_3` (Projections.lean:1309).  vn.tex:2519-2522 (item 3 of `ceil-floor-basic`) is bare: the "Use this to prove…" hint in the same exercise belongs to item 2 (vn.tex:2511-2518), not item 3.  Both clauses stated.
- **DEFECT — avn-projections.csv:51, 56XVI `exists_projSup` (Projections.lean:1427).  `proof` should be `faithful`.**  The point (vn.tex:2540-2552) is an Exercise that prints an argument: "Deduce from this result [56XIV]…" plus "(Hint: use `\sref{ceil-floor-basic}`, and the fact that `p ↦ p^⊥` is an order anti-isomorphism…)" at vn.tex:2549-2551.  The Lean does exactly that: `D` = the projections below every projection upper bound, directed via the binary join `isLeast_ceil_add` (56XIII.2 `ceil-floor-basic`), then `vna_directed_supremum_projections` (56XIV) at Projections.lean:1442-1466.  `stmt` stands.  *Lead outside the sample:* :53 `exists_projInf` sits under the same printed hint and its own note says "ours follows the hint's second half" while grading `none`.
- CONFIRMED — avn-projections.csv:67, 59III.1 `ceil_le_iff` (Projections.lean:1694).  vn.tex:2730-2736 (`ceil-basic` item 1) is a bare Exercise item — no proof, no hint, no solution.  The third equivalent `p·a = a` is on the sibling `ceil_basic_1`.
- CONFIRMED — avn-projections.csv:68, 59III.1 `ceil_basic_1` (Projections.lean:2435).  Same bare exercise item; the Lean gives the full `TFAE [p*a = a, a*p = a, ceil a ≤ p]`, the print's three equivalents exactly, with the parenthetical "in particular ⌈a⌉ is the least such p" on `ceil_spec` (:893).
- CONFIRMED — avn-projections.csv:74, 59IV `ceil_mul_eq_zero` (Projections.lean:1621).  Not the printed claim: 59IV.1 (vn.tex:2751-2755) asks for `⌈a₊⌉⌈a₋⌉ = 0` with the hint "recall that `a₊a₋ = 0`", and that hint is discharged on the sibling row :75 `ceil_pos_part_1`.  This is the auxiliary rule `b·c = 0 ⟹ ⌈b⌉c = 0`, proved by 44VIII on the chain `b^{1/2ⁿ}` — none of the printed hint.
- CONFIRMED — avn-projections.csv:90, 60VII.1 `ceil_fundamental_1` (Projections.lean:2946).  vn.tex:2905-2912 prints "Deduce from `\sref{ncp-ceil}` that `⌈a*ba⌉ = ⌈a*⌈b⌉a⌉` when `b ≥ 0`"; the Lean statement is that equation verbatim and the proof is one term, `ncp_ceil (conjPMap a) …` — our 60V transcription applied to `x ↦ a*xa`, normality via 44VIII.
- CONFIRMED — avn-projections.csv:111, 63IV `cp_comprehension` (Projections.lean:3790).  vn.tex:3106-3111 (Lemma) with proof at vn.tex:3112-3140; the Lean is stated at the ambient C*-algebra variables with `p ∈ effects A`, `f (1-p) = 0`, all three equations, and the proof runs the two printed paragraphs in order — `(p⊥)² ≤ p⊥` then Kadison via `omega_norm_basic_1` for the scalar case, then `states_order_separating_2` for general `B`.
- CONFIRMED — avn-projections.csv:116, 64II `abelian_projections_norm_dense_sub` (Projections.lean:4647).  A `private theorem` transporting the Proposition (vn.tex:3161, proof at :3164-3197) to a commutative von Neumann *subalgebra*; its proof contains none of the printed Gelfand/Stone–Weierstraß argument — it gives `↥S` a `CommCStarAlgebra` structure, calls the real transcription `abelian_projections_norm_dense` (:4320, its own row) and pushes the span forward.
- **DEFECT — avn-projections.csv:121, 65III.3 `commutant_basic_3` (Projections.lean:4433).  `proof` should be `faithful` (`mild` at the outside).**  vn.tex:3237-3239 prints, for exactly this claim, "(Hint: compute `{(0 1; 0 0)}^□` in `M₂`.)" — a printed hint, which the 2026-09-05 convention counts as a printed argument, so `none` is not available.  The Lean uses that very witness (`e = !![0,1;0,0]` in `CStarMatrix (Fin 2) (Fin 2) ℂ`, `e ∈ {e}^□` but `star e ∉`, by comparing the `(0,0)` entries of `e·star e` and `star e·e`): the hint's route.  `mild` is the outside reading only because it shortcuts the full computation of the commutant to the one entry.  `stmt` stands.
- **DEFECT — avn-projections.csv:132, 66IV.3 `ultracyclic_basic_3, ultracyclic_basic_3_directed` (Projections.lean:4871, :4931).  `proof` should be `route`.**  vn.tex:3350 prints "(Hint: first consider `p = 1`.)" for part 3, so a printed argument exists and `none` is wrong.  The declaration's own doc comment (:4857-4866) says "*Class 2 — different route.* The thesis's hint is 'first consider p = 1'; we do not need the reduction" — leastness is proved for general `p` directly by compressing an arbitrary `τ` to `conjNP p τ`.  The earlier `route → none` reclassification of 2026-08-26 was made on the now-superseded ground that a hint is not an argument.  `stmt` stands.
- CONFIRMED — avn-projections.csv:138, 42V `instVNSubtype` (Projections.lean:4616).  vn.tex:262 `von-neumann-examples` item 4 (lines 296-305) defines a von Neumann subalgebra and asserts parenthetically "(and is itself a von Neumann algebra)" with no proof, no hint, no sibling point and no erratum.  The `local instance` builds `VonNeumannAlgebra ↥S` from `Fact (IsVNSubalgebra A S)` exactly as item 4 asserts — suprema computed in `A` land in `S` via `dirSup_mem`, faithfulness by restricting the np-functionals.
- CONFIRMED — avn-projections.csv:141, 67IV.1 anonymous instance `CStarAlgebra CentralProj.sub` (Projections.lean:5294).  vn.tex:3407-3428 is an Exercise with no proof and no hint anywhere in parsec 670, and no erratum.  The instance gives the corner `c.sub = {a : ca = a}` (with `1 = c`) its C*-structure, the first half of demand (ii); the rest is the sibling `VonNeumannAlgebra c.sub` at :5439.  Locator staleness only: the row cites `:5179`, which is now inside `central_examples_3` — a note imprecision, not a graded column.
- CONFIRMED — avn-projections.csv:154, 68IV.1 `cceil_basic_1` (Projections.lean:6438).  vn.tex:3489, item 1, prints no hint; no sibling point of parsec 680 argues it, and the `parsec-680.40` erratum (asols.tex:357) only fixes the wording of *item 2*, supplying no argument.  All three equalities stated.
- CONFIRMED — avn-projections.csv:160, 69III `ceil_mem_ideal_of_effect` (Projections.lean:6892).  vn.tex:3546-3557 prints "it suffices to show `⌈a⌉ ∈ 𝒟` … which, in turn, follows from `⌈a⌉ = ⋁ₙ a^{1/2ⁿ}`"; the Lean proves that sub-claim, keeping the printed shape (an increasing family in `𝒟` with sup `⌈a⌉`, plus directed-sup-closure) and swapping only the family, `1−(1−a)^{2ⁿ} = (∑_{i<2ⁿ}(1−a)^i)·a`, because the printed roots step is circular (`ERRATA.md`:42, OPEN).  One documented step replaced inside the printed argument, not a different route: `mild` is right.
- CONFIRMED — avn-projections.csv:162, 69IV `carrier_eq_cceilMap` (Projections.lean:7136).  vn.tex:3610-3616 Corollary: "The carrier `⌈f⌉` … is central, **so** `⌈f⌉=⌈⌈f⌉⌉`".  The Lean states that equation and proves it from the centrality supplied by `carrier_miu`, by leastness of `cceil` one way and `⌈⌈p⌉⌉p=p` the other — the printed "so".  The other two clauses are on the named sibling `carrier_miu`.
- CONFIRMED — avn-projections.csv:189, 70III `cvn_linfty_normal` (Projections.lean:8420).  `stmt` re-derived from vn.tex:3757 directly: the Lean existential carries every element of the Theorem — index family, finite (`IsFiniteMeasure`) complete (`IsComplete`) measure spaces, each `L^∞(Xᵢ)` rendered as 51IX renders it, and a bijective `NMIUMap`.  The audit's standing "headline rendered by a witness" worry does not bite here: nothing is stated only via a consequence, and the presentation form *is* the tree's `L^∞`.  The proof is 70IV's three printed steps (`cvn_direct_sum` = 70II at `c=1` plus 67IV.2, then 54XI via `exists_linftyPresentation_normal`).
- CONFIRMED — avn-projections.csv:196, 67III `le_smul_one_iff_posPart_eq_zero, smul_one_le_iff_negPart_eq_zero` (Projections.lean:8689, :8694).  67III is the Remark at vn.tex:3397 defining "factor" — no proof, no hint, no solution, no erratum.  These are auxiliary CFC glue (`CFC.posPart_eq_zero_iff` against `sub_nonpos`, and dually), not the printed point; `ok` matches the file's uniform treatment of the auxiliaries at rows :191-:195 and :197-:198.
- CONFIRMED — avn-projections.csv:200, 67III `eq_algebraMap_of_central_proj_trivial` (Projections.lean:8818).  Same anchor Remark.  The declaration is the *criterion* for factoriality, not 67III's definition, and its proof (realPart/imaginaryPart split, `IsCentral.posPart`/`IsCentral.ceil`, then the spectral scale) is entirely its own.
- CONFIRMED — avn-projections.csv:205, 67III `IsMinimalProjection.central_proj_below_cceil` (Projections.lean:8897).  The print is bsols.tex:3366-3370 (solution to eff.tex 224VI): "as `p𝒜p ≅ ℂ` both have only two projections and so `p` is a minimal projection.  In particular `0` is the only central element strictly below `p`, thus `⌈⌈p⌉⌉𝒜` is a factor."  The Lean runs on that mechanism — for central `q ≤ ⌈⌈p⌉⌉`, `q*p` is a projection below `p`, so `0` or `p` by minimality — so `faithful` rather than `none`; the projection/central-element split is a rendering choice, the passage back carried by `eq_algebraMap_of_central_proj_trivial` and the composite `IsMinimalProjection.isFactor_cceil` (:8987).

## Counts and the ten defects

**Counts: 120 CONFIRMED, 10 DEFECT, 0 UNSURE** — an error rate of 7.7% on the
`ok` grade in this draw (the first 2026-09-05 draw measured 9 in 123, 7.3%;
the 2026-09-04 draw, graded before the hint convention existed, measured 0 in
44).

All ten defects are in the **`proof`** column.  **No sampled `stmt = ok` grade
was found wrong** — the second draw running to 130 rows without a single
statement defect, as the first draw did at 123.

Eight of the ten are the same failure mode, and it is the one the 2026-09-05
convention was written to fix: **a printed argument — most often an Exercise's
hint, sometimes a named step inside a printed proof — graded as though nothing
were printed.**  Six of those eight follow the printed argument and should be
`faithful` (`aproc-duplicators-quantumlambda.csv:53`, `:110`,
`aproc-tensor.csv:71`, `:98`, `avn-basic.csv:43`, `avn-projections.csv:121`);
two do not follow it and should be `route` (`avn-basic.csv:88`,
`avn-projections.csv:132`).  The remaining two are ordinary route defects
graded `faithful` (`acstar-matrices-representation.csv:127`) — inherited from a
sibling the first draw already regraded — and, at `avn-projections.csv:132`,
one whose own doc comment announces the divergence in so many words.

The defect lines:

1. **acstar-matrices-representation.csv:127**, 27XVII `spectrum_eq_range_elemental` — `proof` should be `route`: the printed solution `parsec-270.170` argues via 27XV `inv-mult-state`, the Lean via Mathlib spectral permanence plus a Gelfand-theorem lemma that uses 27XV nowhere.  Its sibling `spectrum_eq_range_char` (:124) was regraded `route` on the same ground by the first draw.
2. **aproc-duplicators-quantumlambda.csv:53**, 132III `exists_nmiu_mul` — `proof` should be `faithful`: 132III.2 (proc.tex:6683) prints "Deduce from this and `\sref{duplicable}`", and the declaration is that deduction.  The same point's part-2 row is already `faithful` off that very hint.
3. **aproc-duplicators-quantumlambda.csv:110**, 125II `card_le_of_denseRange` — `proof` should be `faithful`: it renders the printed step "every element of `ℋ_ω` is the limit of a sequence of elements from `𝒜`" (proc.tex:4825) by exactly that argument; the analogue for 124I in the same file is graded `faithful`.
4. **aproc-tensor.csv:71**, 112X `orderSeparating_unital_norm` — `proof` should be `faithful`: 112X.2 prints this step with its citation (proc.tex:2911-2919, `\sref{order-separating-norm}`), and the Lean follows it.
5. **aproc-tensor.csv:98**, 115II `exists_npExtension_conjProdNP` — `proof` should be `faithful`: proc.tex:3208-3218 supplies both halves of the printed `ω'` with their reasons, and the Lean proves both from those reasons.
6. **avn-basic.csv:43**, 43II `vn_counterexamples_5` — `proof` should be `faithful`: 43II.5 prints a route ("…**by proving that** `(|0⟩⟨n|)ₙ` has no ultrastrongly convergent subnet"), which our own `ERRATA.md`:37 repairs to `(|n⟩⟨0|)ₙ` and records that this declaration "silently carries the repair".
7. **avn-basic.csv:88**, 46III `preservesDirSups_of_continuous_ultrastrong` — `proof` should be `route`: 46III prints "(Hint: combine `\sref{p-uwcont}` and `\sref{cp-uscont}`.)" and neither this implication nor the `npuws` shell touches 44XV or 45II; the proof goes directly through 44XIV.  (Read instead against 45I's hint, which the doc comment invokes, it would be `faithful` — the same shape the first draw filed against `us_cont_normal` at :81.  `none` is wrong either way.)
8. **avn-projections.csv:51**, 56XVI `exists_projSup` — `proof` should be `faithful`: vn.tex:2549-2551 prints "Deduce from this result…" plus a two-part Hint, and the Lean runs both parts (`isLeast_ceil_add` for directedness, then 56XIV).
9. **avn-projections.csv:121**, 65III.3 `commutant_basic_3` — `proof` should be `faithful` (`mild` at the outside): vn.tex:3237-3239 prints "(Hint: compute `{(0 1; 0 0)}^□` in `M₂`.)" and the Lean uses that exact witness.
10. **avn-projections.csv:132**, 66IV.3 `ultracyclic_basic_3` — `proof` should be `route`: vn.tex:3350 prints "(Hint: first consider `p = 1`.)" and the doc comment says in terms "we do not need the reduction".  This row was moved `route → none` on 2026-08-26 on the ground the convention has since overturned.

### Leads outside the sample, from neighbours read while checking

- `avn-division-normalfunctionals.csv:53` (81I `exists_div`) grades the parenthetical "(by `\sref{mult-cancellation}`)" as `faithful` where `:3` grades the identical parenthetical `none`; one of the two is wrong, and it is not `:3`.
- `avn-projections.csv:53` (`exists_projInf`) sits under the same printed hint as the defective `:51`, and its own note says "ours follows the hint's second half" while grading `none`.
- `aproc-measurement.csv:139` (104VII `ceil_mul_proj_mul_of_comm`) has a note reading "step 1 of the author's proof (proc.tex:1560)" while graded `none`, where `:140` covering "steps 1-2" is graded `faithful`.
- `aproc-tensor.csv:137` (`sum_generation_1_is_false`) is the twin of `:118`: under this file's non-transcription convention both are `ok`, under the brief's literal `stmt` rule both would be `differs`.  One convention decision, two rows — worth an explicit ruling rather than a per-row finding.
- The population most likely to hold more of defect-type 1 is the rows moved to `none` by the **2026-08-26 A/VN route pass**, whose stated ground (a hint is not an argument) the 2026-09-05 convention has reversed.
