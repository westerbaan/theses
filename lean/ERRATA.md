# Errata — defects in the theses, for processing

One line of context per item, ordered by point number within each thesis, so
this file can be worked through top to bottom.  **Status** is `OPEN` (not yet
acted on by an author) or `DONE` (fixed in the sources; erratum key given where
one exists).

Scope: **only defects in the theses**.  Our own mis-transcriptions are *not*
here — they are in [PROVING-LOG.md](PROVING-LOG.md), since they need no author
attention.  Items needing an author **decision** rather than a correction are in
[QUESTIONS.md](QUESTIONS.md).

Locate by **point number**, never by line: `file:LINE` references drift every
time the sources are edited.

---

## Thesis A — `cstar.tex`

| # | defect | fix | status |
|---|---|---|---|
| **4VIII** | inner product typed `⟨x,·⟩ : V → V` | codomain is `ℂ` | DONE `40.80` |
| **11VI.2** | bound stated as `‖a‖ < ‖b‖` | should be `‖a‖ < ‖b⁻¹‖⁻¹` | DONE |
| **11XV.2** | "invertible **by point 2**" is a self-reference | it is point 1, cited correctly 30 lines later | DONE (solution) |
| **11XV.3** | hint `aⁿ+1 = ∏(a+ζ^{2k+1})` has the wrong sign | RHS is `aⁿ−1`; should be `∏(a−ζ^{2k+1})` | DONE (pre-existing) |
| **11XV.3** | "`ζ^{2k+1} ∉ ℝ` when `k ≠ ½(n−1)`" | should be `k ≢ (n−1)/2 mod n`; literally false at `n=1` | OPEN (harmless) |
| **11XX.1** | `f : X → ℝ`, but `C(X)` is the complex-valued functions | `f : X → ℂ` | DONE `110.200` |
| **11XX.2** | "invertible iff its kernel is **not** `{0}`" — dropped negation | `A = 0` in `M₁` refutes it as printed | DONE (solution) |
| **12III.3** | `z ∈ 𝒜` | should be `z ∈ ℂ` | DONE `120.30` |
| **16V** | false in the trivial algebra `{0}` (every element a unit, `spec = ∅`) | hypothesise `𝒜 ≠ {0}` | DONE `160.50` |
| **16VI** | same defect in the `←` direction | reworded: `spec(a) ⊆ {λ}` iff `a = λ` — true in `{0}` too | DONE `160.60` |
| **17III** | stated for `t ∈ [0,∞]`; at `t = ∞` the condition is meaningless | `[0,∞)` | DONE `170.30` |
| **17VI.3** | `inf` over `λ ∈ ℝ` is `−∞` in `{0}` | `inf` over `λ ≥ 0` | DONE `170.60` |
| **17VI.6** | "suppose `a` is **not** invertible" inverts the direction proved | delete the "not" | DONE (solution) |
| **19Ia** | `λ⁻¹(1 + b(λ−ab)a)` missing an inverse | `λ⁻¹(1 + b(λ−ab)⁻¹a)` | DONE `190.20` |
| **20aII** | "`e ∘ h = g`" | should be `γ` | DONE (solution) |
| **20II.1** | proof needs `f a` self-adjoint, never stated; not part of `IsPositiveMap` | derive from `a = a⁺ − a⁻` | OPEN |
| **22III.5** | false in `{0}`; the solution also picks a convergent subsequence of `spec(a)`, assuming it non-empty | hypothesise `𝒜 ≠ {0}`, cite 16V | DONE `220.30` |
| **22VIII** | state-existence half false in `{0}` | hypothesise `𝒜 ≠ {0}` | DONE `220.80` |
| **23II** | cites `ineq-square-root` where commuting products are handled | cite `square-commuting-monotone` (230.40) | DONE `230.60` |
| **23VII.3** | "commuting `a ≤ b` ⟹ `a² ≤ b²`" false: `a = −2 ≤ 1 = b` in `ℂ` | assume `0 ≤ a ≤ b` | DONE `230.70` |
| **26II.1** | "`a` and `b` are positive" — `a` is arbitrary self-adjoint; and `b ≥ 0` is used but not derived | add the step `2b = (b−a)+(b+a) ≥ 0` | DONE (solution) |
| **26II.4** | `a ∨ b = ½(a+b+|a+b|)` | should be `|a−b|`; check `a=2, b=−1` | DONE (solution) |
| **30IV.1** | Cauchy–Schwarz displayed with squares on the right | as printed it does not give Kadison's inequality | DONE (solution) |
| **30IV.2** | spurious `‖ω‖` factor | Mathlib's `leftMulMapPreGNS` has bound exactly `‖a‖` | DONE `300.40`/`300.60` |
| **32I** | definiteness stated once, but used in the *first* argument for module-linearity and the *second* for uniqueness of the adjoint | state both | OPEN |
| **33I.2** | the surjectivity half never uses the adjointability hypothesis | redundant, harmless | OPEN |
| **34aVII** | Russo–Dye false at `N = 0` (Lean's `2/0 = 0` makes the hypothesis vacuous) | "for some natural number `N > 0`" | DONE `341.70` |
| **34VI.1** | solution slot `parsec-340.60` is an empty `\TODO{}` | — | OPEN |
| **34XVI** | derives `cp-russo-dye` from Russo–Dye (**34aVIII**), a *later* point | forward reference; we derive it from 34XIV instead | OPEN |
| **39VII** | `ω(A) = ∑_{e,e'∈E} ⟪e,Ae'⟫ ω(\|e⟩⟨e'\|)` is **false** if `∑` means the unordered sum of 6II: the family need not be absolutely summable (`ω = ⟪x,(·)x⟫` on `ℓ²`, `A` block diagonal with `N_k×N_k` DFT blocks, `N_k = k⁸`, `x` constant `k^{-5}` on block `k`) | read it as the limit of the **square** partial sums `∑_{e,e'∈F}`, `F ⊆ E` finite — which is what the proof establishes and what 39IX uses; our statement is realigned and **proved** | OPEN |
| **62I** | cites `inner-product-basic` for `f(a)² ≤ f(a²)` | should cite `cp-cs` | DONE `620.20` |
| **9II** | on `X = ∅` the "iff" in the `sup ∅` step fails for `t < 0` | harmless — only two directions are used | OPEN (nit) |
| **7III.8 / 7III.13 / 9X.3** | counterexample solutions open "let `x,y` be … vectors" without exhibiting one | needs `dim ℋ ≥ 2`; `ℂ²` works in all three | OPEN (nit) |
| **9X.3** | divides by `‖P − ‖x‖²‖` without excluding zero | degenerate case is fine anyway | OPEN (nit) |

## Thesis A — `vn.tex`

| # | defect | fix | status |
|---|---|---|---|
| **37IX** | "is WOT-Cauchy **and WOT-bounded**" does not follow: 37VII needs *norm*-boundedness, 37IX supplies only a bound from above (`D = {−n·1}`) | statement gains "non-empty"; proof squeezes boundedness, gets self-adjointness via 25V(1). **37VII deliberately unchanged** | DONE `370.90`/`370.100` |
| **37IX** | omits `D ≠ ∅`, needed by parts 1 and 2 | — | DONE (as above) |
| **37IX** | proof claims 37VII yields a *self-adjoint* limit; 37VII concludes only "some bounded operator" | — | DONE (as above) |
| **38VI.2** | `←` direction false: a constant net `x_α = i·x` gives the same vector functional | direction dropped; our statement realigned and the surviving implication **now proved** | DONE `380.60` |
| **44III** as cited by **44VII/44XIV** | cited as a black box, but it demands *every* `xᵢ` be an effect, while `(⋁D − d)/M` is only *eventually* one — a bounded directed set need not be bounded below | needs a cofinal-tail footnote once nets are made precise | OPEN |
| **61II** | both displayed inequalities point the wrong way | reversed, as is the proof's last line | DONE `610.20` |
| **68IV.2** | clauses 1 and 3 stated without positivity; central support is monotone only on positives (`D = {−1,0}`; `a=1, b=−1`) | positivity added to both | DONE `680.40` |
| **72III.1b/1c** | spurious `‖ω‖`: `‖a‖_ω` is unnormalised, so the two sides scale as `t` and `t²` | factors deleted | DONE `720.30` |
| **72V.4** | the fourth clause of the equivalence — `f = f₀+if₁−f₂−if₃` with the `f_k` np-maps **dominated by `Bω` on `𝒜⁺`** — is *strictly stronger* than the other three, so the lemma is false as an equivalence. `B(ℂ²)`, `ω = ⟪ξ,(·)ξ⟫`, `f = ⟪η,(·)ξ⟫` for `ξ ⊥ η` unit: `\|f(a)\| ≤ ‖a‖_ω`, yet for `p = \|η⟩⟨η\|` one has `ω(p) = 0`, so every dominated np-map kills `p𝒜` (Cauchy–Schwarz) while `f` does not. The proof's last paragraph is where it goes wrong: `ω(c*ac) ≤ ‖c‖_ω ω(a)` on `𝒜⁺` is not homogeneous in `ω` and holds for no constant (`M₂`, `ω(a)=a₁₁`, `c=e₂₁`, `a=e₂₂`) | drop clause 4 from the equivalence; keep it only as the *sufficient* condition 4 ⇒ 1, and state 3 ⇒ 4 without the domination. 72XI is unaffected — it dominates against `ω' = Σ_k f_k`, not against the given `ω`. Both surviving halves are formalized and proved | OPEN |
| **74V** (the proof of 74IV, `kaplansky`) | the general case asserts that `B = [[0,b],[b*,0]]` is the *ultrastrong* limit of `[[0,aα],[aα*,0]]`; this needs `aα* → b*` ultrastrongly, which does not follow — the adjoint is not ultrastrongly continuous, by the thesis's own **43II**.4 | take the **ultraweak** limit instead (the adjoint *is* ultraweakly continuous), putting `B` in the ultraweak closure of the convex set `M₂(𝒜)`, and move to the ultrastrong closure by **73VIII** `ultraclosed` — the same step the self-adjoint case makes three paragraphs earlier.  Repaired proof formalized; 74IV itself is unaffected | OPEN |
| **74VII** (the proof of 74VI, `dense-subalgebra`) | three slips in the last two sentences: `s_{αn}` is said to converge to `b`, but 74VI has no `b`; and `‖c_α‖ ≤ ‖c‖ ≤ (1+ε)‖c‖` uses an undefined `c`.  With the intended reading (`a` throughout) the final step still fails when `‖a‖ = 0`: `lim_n‖s_{αn}‖ = 0` does not make `‖s_{αn}‖ ≤ 0` for large `n` | read `a` for `b` and for `c`; treat `a = 0` separately (`a = 0 ∈ 𝒮`, constant net).  Formalized with the `a = 0` case split | OPEN |
| **75III** | proof asserts `0 ≤ a_nm ≤ ½` | should be `0 ≤ a_nm ≤ 1` | DONE `750.30` (solution) |
| **75IX** (the proof of 75VIII, `vnsac`) | uses two facts stated nowhere in the thesis: (a) "the ultrastrong closure of `𝒮` is a von Neumann subalgebra" (asserted in the sentence beginning "because the ultrastrong closure of `𝒮` …"); (b) the *dual* of **66IV**.3, `p = ⋀_ω ⌈ω⌉^⊥` over `ω` with `ω(p) = 0`, used in the displayed chain | (a) is true and deserves a statement of its own: the closure is convex and ultrastrongly closed, hence norm-closed (the ultrastrong topology is coarser than the norm one) and — by **73VIII** `ultraclosed` — ultraweakly closed, which gives star-closedness for free (the adjoint is ultraweakly, not ultrastrongly, continuous); multiplication needs only *separate* ultrastrong continuity, `‖ax‖_ω ≤ ‖a‖‖x‖_ω` and `‖xb‖_ω = ‖x‖_{b*ω}`; closure under directed suprema is **44XIV**.  (b) is **not needed at all**: complementing the right-hand half of the display turns `⋀_{ω₀} ⌈ω₀⌉^⊥` into `⋁_{ω₀} ⌈ω₀⌉ = p^⊥`, i.e. **66IV**.3 applied to `p^⊥`, so the proof uses one lemma where it now uses two.  Also, `⌈ω⌉_𝒮` needs no von Neumann structure on `𝒮`: defining `⌈ω⌉_𝒮^⊥` as the join of the projections of `𝒮` killed by `ω`, both of its defining properties come from one directed-supremum argument (`⌈x+y⌉` is in `𝒮` by 56I.20 and is killed by `ω` by **60I**).  Formalized in the shortened form | OPEN |
| **77II** (the proof of 77I, `vn-complete`) | justifies "the ultrastrong topology on `B(ℋ_Ω)` coincides on `ℛ` with the ultrastrong topology of `ℛ`" by "any np-functional `ω : ℛ → ℂ` is of the form `⟨x,(·)x⟩` for some `x ∈ ℋ_Ω`".  As written this is (a) a **forward reference**: a claim about *every* np-functional of `ℛ`, which is the content of **89IX** `normal-functional`, twelve parsecs later and itself resting on 77I's neighbourhood; and (b) *stronger than 89IX delivers*, since 89IX gives a **sum** `∑ₙ⟨xₙ,(·)xₙ⟩`, not a single vector.  The same sentence is re-used for the ultraweak half | neither is needed.  Only one direction of the "coincides" is used, and it holds **by construction of `ρ_Ω`**: with `Ω` the set of *all* np-functionals of `𝒜`, every np-functional of `𝒜` is a vector functional in `ℋ_Ω` — this is exactly **48V** `varrho-Omega-normal`, already available at parsec 770.  Cite 48V in place of the quoted sentence.  The converse direction, that an np-functional of `B(ℋ_Ω)` restricts to one of `𝒜` along `ρ_Ω`, is elementary and needs no comment (`ω ∘ ρ` is positive because `ρ` is a ∗-homomorphism and normal because `ρ` is).  Formalized this way: `vn_complete_1`/`vn_complete_2` use 48V, **75VIII** and **76I**/**76III** only | OPEN |
| **79VI.4** (`pseudoinverse-basic-2`) | **false as stated**: `c^{∼1} ≤ b^{∼1}` for pseudoinvertible positive *commuting* `b ≤ c` fails whenever `⌈b⌉ < ⌈c⌉`. Machine-checked as `pseudoinverse_basic_2'_4_is_false` in `A/VN/Division.lean`: in `ℓ^∞({0,1})` take `b = (1,0)`, `c = (1,1) = 1` — projections, so both are their own pseudoinverses — and `c^{∼1} = 1 ≰ (1,0) = b^{∼1}`. Not a degeneracy of the model: the same argument runs in *any* von Neumann algebra with a projection `p ≠ 1`, taking `b = p`, `c = 1` (`pseudoinverse_basic_2'_4_forces_eq_one`). The parenthetical "(The statement is still true without the requirement that `b` and `c` commute…)" is false for the same reason | add `⌈b⌉ = ⌈c⌉` to the hypotheses (equivalently: work inside the corner `⌈c⌉𝒜⌈c⌉`, where both are honestly invertible and the classical `c⁻¹ ≤ b⁻¹` applies). Our statement is kept verbatim and `sorry` pending the author's choice of repair | OPEN |
| **83V** (the proof of `cceil-sum`) | last sentence: "In other words, **`e`** could have been added to `(e_i)_i`, contradicting its maximality" — the projection just constructed is `u^*u`, not `e`, and `e` is in general neither non-zero-orthogonal to the `e_i` nor a member of the family | read `u^*u` for `e`.  Two further gaps of detail in the same proof, both harmless: the chain `e_i = u_i^*u_i \leq u_i^*eu_i \leq \bigcup_a \ceil{a^*ea}` needs a ceiling at the second step (`u_i^*eu_i` is not a projection), and the equality `\bigcup_a \ceil{p\ceil{a^*ea}p} = \bigcup_a \ceil{(eap)^*eap}` uses `\ceil{p\ceil{c}p} = \ceil{pcp}`, which the thesis does not state.  Formalized with the second replaced by its contrapositive, which needs neither | OPEN |
| **89I** | "`UU* = FF*` is the projection onto `closure(π(A)y)`" | must read `U*U = FF*` | OPEN |
| **89III** | declares `Uᵢ : H → K`, then swaps the two projections and later writes `⟨x, Uᵢy⟩` as if `Uᵢ : K → H` | make the typing consistent | OPEN |
| **99VII** | three `a`-for-`b` slips at 61II's sole use site | corrected paragraph | DONE `990.70` |

## Thesis A — `proc.tex`

| # | defect | fix | status |
|---|---|---|---|
| **98VI** | the hint gives `⌈τ⌉ ≤ ⌈π(⌈τ∘π⌉^⊥)⌉^⊥`, which is a restatement of `τ(π(r^⊥)) = 0` — the direction one does *not* need | the proof needs the converse; both hold in the model | OPEN |
| **101II** (proof, point 30) | "`⌈ef(·)e⌉` is the **greatest** projection `s` with `ef(s^⊥)e = 0` (see `carrier`)" — 63I defines the carrier as the **least** such projection, and the point being proved asserts a *least* projection, so the proof contradicts both its own citation and its statement | read "least" for "greatest"; everything else in the proof is correct (the Lean proof is a literal transcription) | OPEN |
| **101VII.2** (`equivalent-examples`) | "An ncp-isomorphism `f : 𝒜 → ℬ` is contraposed to its inverse `f⁻¹`" is **false without unitality**, and the thesis explicitly admits non-unital ncp-isomorphisms (proc.tex:282, "there are corners which are not unital, because there are non-unital ncp-isomorphisms"). Counterexample, machine-checked as `equivalent_examples_2_is_false` in `A/Proc/Measurement.lean`: in `B(ℂ²)` take `a = diag(1,2)`, `f = a(·)a`, `f⁻¹ = a⁻¹(·)a⁻¹` (both ncp by `ad-ncp`), `s = ` the projection onto `ℂ(1,1)`, `t = ` the one onto `ℂ(2,−1)`. Then `⌈f(s)⌉ = ` proj onto `ℂ(1,2) ⊥ t`, so `⌈f(s)⌉ ≤ t^⊥`; but `⌈f⁻¹(t)⌉ = ` proj onto `ℂ(4,−1)`, which is **not** `⊥ s`. (By part **1** of the same Examples, `a(·)a` is contraposed to `a(·)a*= a(·)a`, i.e. to *itself* — and a contraposition partner is unique up to `⋄`, so it cannot also be contraposed to `f⁻¹`.) | add "unital": for a *unital* ncp-isomorphism the claim is true and short (`equivalent_examples_2`, now proved — `f` and `f⁻¹` are then unital order isomorphisms, hence map projections to projections, and `f(s) ≤ 1−t ⟺ s ≤ 1−f⁻¹(t)` by applying `f⁻¹` and `f`). No unitality is needed anywhere else in parsec 1010, and an isomorphism of `W*_cpsu` is automatically unital (`1 = f⁻¹(f(1)) ≤ f⁻¹(1) ≤ 1`), so nothing downstream changes | OPEN |
| **104III.2a** | **all three claims are false as printed** — the exercise omits the faithfulness hypothesis. Counterexamples ((ii) and (iii) machine-checked in `A/Proc/Measurement.lean`, (i) by hand): (i) *"`p ~ q` iff `p/q` is central" (⇒)*: take `p = q = e` for any non-central projection (`e = diag(1,0)` in `M₂`); `p ≤ 1·q` and `p ~ q` by reflexivity (`c = d = 1`), but `p/q = e/e = ⌈e⌉ = e` is not central. (ii) *"`p ~ 1` iff `p` is central" (⇐), and the same claim at `q = 1` in (i)*: any central `p` with `⌈p⌉ ≠ 1` — `p = 0` in `ℂ`, or `p = (1,0)`, `q = (1,1)` in `ℂ²` — has `p` and `p/q` central and `p ≤ B·q`, but part **2** of the same exercise forces `⌈p⌉ = ⌈q⌉`, so `p ≁ q` (`centrally_similar_basic_2a_counterexample`). (iii) *"`p ~ p²` iff `p` is central" (⇒)*: every projection has `e² = e`, hence `e ~ e²` with `c = d = 1`, so the claim would make **every** projection central (`centrally_similar_sq_of_isStarProjection`) | add the hypothesis `⌈p⌉ = ⌈q⌉ = 1` (i.e. `p`, `q` faithful) — exactly what **104VII** `positive-quotients-centrally-similar` states explicitly two points later. With it all three go through: for (ii)⇐ take `c = 1`, `d = p`; for (i)⇐ take `c = 1`, `d = p/q`, whose carrier is `1` because `⌈p⌉ = ⌈(p/q)q⌉ ≤ ⌈p/q⌉`. `⌈p⌉ = ⌈q⌉` alone is **not** enough — counterexample (i) satisfies it | OPEN |
| **104IV** (`centrally-similar-fundamental`) | the hypothesis **`⌈q⌉ = 1` is missing from the statement** although the proof uses it explicitly ("`ϑ(e)q = qe = eq` *and `⌈q⌉ = 1`* imply that `ϑ(e) = e` by `mult-cancellation`"). Without it the second conclusion is false: at `q = 0` both hypotheses `⌈qϑ(e)q⌉ ≤ e` and `⌈qϑ(e^⊥)q⌉ ≤ e^⊥` read `0 ≤ e`, `0 ≤ e^⊥` and hold for *every* projection `e` and *every* miu-map `ϑ`, so the lemma as printed would make every miu-endomorphism of every von Neumann algebra fix every projection — false for `Ad_u` on `B(ℂ²)`, or the flip on `ℂ ⊕ ℂ`. Machine-checked as `centrally_similar_fundamental_needs_faithful` in `A/Proc/Measurement.lean`. The first conclusion `eq = qe` is unaffected | add `⌈q⌉ = 1` to the hypotheses. Both consumers (**104VI**, **104VII**) state it, so nothing downstream changes; with it the printed proof goes through verbatim (`centrally_similar_fundamental`, proved) | OPEN |
| **117II.1** (`sum-generation`) | **false as printed**: `κᵢ` is not unital, so nothing puts `eᵢ = κᵢ(1)` into `W*(⋃ᵢ κᵢ(Aᵢ))`. Machine-checked as `sum_generation_1_is_false` in `A/Proc/Tensor.lean`: take `I = {0,1}`, `𝒜₀ = 𝒜₁ = ℂ`, `A₀ = A₁ = ∅` — every subset of `ℂ` generates `ℂ`, since every ∗-subalgebra of `ℂ` is `ℂ` — while `W*(∅) ⊆ ℂ·1 ⊊ ℂ ⊕ ℂ`, the diagonal being a von Neumann subalgebra (closed, and closed under directed suprema because those are coordinatewise). Not a degeneracy of the empty set or of `ℂ`: `A₀ = A₁ = {0}` works verbatim, and `𝒜₀ = 𝒜₁ = ℂ²` with `A₀ = A₁ = {(1,0)}` gives a counterexample with nontrivial summands, non-empty generating sets and `W*(⋃ κᵢ(Aᵢ))` of dimension 3 inside `ℂ⁴` | add the coprojections: `⋃ᵢ κᵢ(Aᵢ) ∪ {eᵢ : i ∈ I}` (equivalently, require `1 ∈ Aᵢ`). With them the exercise goes through: `{a : κᵢ(a) ∈ W}` is then a *unital* ∗-subalgebra of `𝒜ᵢ`, norm-closed and closed under directed suprema, hence `= 𝒜ᵢ`; and a general `x` is the directed supremum of its finite restrictions `∑_{i∈F} κᵢ(xᵢ)`. Our statement is realigned this way and **is now proved** (session 41), as is **117II.2** | OPEN |
| **129VIII** (proof, point 90) | "either `0 < μ(A) ≤ ½μ(A₁)` or `0 < μ(X∖A) ≤ ½μ(A₁)`" — the second set should be `A₁∖A`, not `X∖A`; the very next sentence ("there is `A₂ ⊆ A₁` with …") needs it inside `A₁`, and the identity being used is `μ(A) + μ(A₁∖A) = μ(A₁)`, quoted correctly one line above | read `A₁∖A` for `X∖A`; the rest of the proof is correct and was transcribed as printed (`continuous_measure_space`, proved) | OPEN |

---

## Thesis B — `eff.tex` / `bsols.tex`

| # | defect | fix | status |
|---|---|---|---|
| **175V.7** (`eff.tex:414`) **and the `exc-dposet` solution** (`bsols.tex:1494`) | **partial associativity used backwards**: the proof writes `b ⋁ c = (a ⋁ d) ⋁ c = (a ⋁ c) ⋁ d`, "so `a ⊥ c`" — i.e. from `a ⊥ (c ⋁ d)` it concludes `a ⊥ c` and `(a ⋁ c) ⊥ d`, where the PCM axioms (174II) state only the forward direction | recoverable: we added `PCM.assoc_left`, deriving the converse from partial commutativity; it is used in several places since | Bas: This is obvious to the reader, no need to spell it out. |
| **177Ia** (`ea-modularity-prop`) | **the Proposition is false as printed** — the existence half fails. Machine-checked counterexample `WrightTriangle.not_ea_modularity_prop` in `B/Eff/EffectAlgebras.lean`: the Greechie loop of order 3 (three blocks `2³` pasted in a triangle, 14 elements; every effect algebra axiom checked by `decide`). There `a₁ ⊥ a₂` and `a₁ ∧ a₂ = 0` exists, but `a₁ ∨ a₂` does **not** — `a₄ᵖ = a₁ ⋁ a₂` and `a₃ᵖ = a₆ ⋁ a₁ = a₂ ⋁ a₅` are incomparable minimal upper bounds. Conceptually: in *any* orthoalgebra `a ⊥ b` forces `a ∧ b = 0`, so the Proposition would make every orthoalgebra an orthomodular poset, which is exactly what the Wright triangle is not. The cited Dvurečenskij–Pulmannová 1.8.2 must therefore carry a hypothesis the thesis drops (lattice-ordered, or `a ∨ b` assumed to exist) | hypothesise that `a ∨ b` exists: then the identity `a ⋁ b = (a ∧ b) ⋁ (a ∨ b)` holds and is proved (`ea_modularity_prop`, realigned, from master's thesis Cor. 16.2 = `msc_cor16_2`). Both consumers survive: **177VI** `orth-ea-is-orthomodular` is in an ortholattice, where both bounds exist (already proved, using exactly this specialisation), and **208III** is in `SPred X`, likewise a lattice (proved without modularity) | Bas: corrected |
| **177Ia** — ✅ **the second correction (`280fa5b`) is right**, with two slips | the statement is now "if `a ∨ b` exists … then `a ∧ b` exists as well", and the lemma is dualised to "if `(x ⊖ c) ∨ (x ⊖ d)` exists then `c ∧ d` exists and `(x ⊖ c) ∨ (x ⊖ d) = x ⊖ (c ∧ d)`". Both match what we proved (`msc_prop15'`, `dual_modularity_lemma`), and the load-bearing step is now sound: the bound `s` quantified over is a *lower* bound, so `s ≤ c ≤ x` and `x⊥ ⋁ s` is defined | **slip 1 — one inequality should not have been flipped.** "Clearly `(x⊥ ⋁ c) ∧ (x⊥ ⋁ d) ≤ x⊥`" must read **`≥ x⊥`**: each of `x⊥ ⋁ c`, `x⊥ ⋁ d` is `≥ x⊥`, so `x⊥` is a *lower* bound of the pair and lies below their infimum. The very next line, `r := ((x⊥⋁c) ∧ (x⊥⋁d)) ⊖ x⊥`, is only defined under `≥`, so the rest of the proof already assumes the corrected direction | Bas: corrected |
| **`berr.tex`, erratum `ea-modularity-prop`** | "it is *not* the case that `a ∨ b` exists if `a ∨ b` does" — the antecedent is a typo | should read "if `a ∧ b` does"; the sentence then says exactly the right thing, and "the converse is true" is what `msc_prop15'` proves | Bas: corrected |
| **177VI**, `eff.tex:573` — the citation still holds but no longer reads as it argues | "Note `a ∧ (a⊥ ∧ b) ≤ a ∧ a⊥ = 0` and so by `\sref{ea-modularity-prop}`…" presents the meet being `0` as the trigger. Under the corrected statement the trigger is the **supremum** existing — free here, since 177VI hypothesises `E` is an ortholattice — and the meet being `0` is what collapses `a ⋁ b = (a∧b) ⋁ (a∨b)` to `a ⋁ b = a ∨ b`. The inference is valid; it just leans on a hypothesis the sentence does not mention | add a clause, e.g. "as `E` is a lattice, `a ∨ (a⊥ ∧ b)` exists". Contrast **208III** (`eff.tex:4657`), which names its ortholattice in the same sentence and reads correctly as it stands | Bas: the zero infimum is not used as hypothesis, but for the identity afterwards. |
| **177VI**, `eff.tex:578` — ✅ **improved by the correction** | "Similar to `\sref{modularity-lemma-proof}` one can show that `(b ⊖ a) ⋁ (b⊥ ∧ a) = ((b ⊖ a) ⋁ b⊥) ∧ ((b ⊖ a) ⋁ a)`" — that is `⋁` distributing over a binary *infimum*. The **old** lemma's proof established distributivity over *suprema*, so "similar to" was a genuine analogy; the **dualised** proof establishes distributivity over infima directly, as the step `x⊥ ⋁ s ≤ (x⊥⋁c) ∧ (x⊥⋁d)` yielding `x⊥ ⋁ (c ∧ d) = (x⊥⋁c) ∧ (x⊥⋁d)` | 177VI's need is now an *instance* of the lemma's own computation rather than an analogy — "Similar to" could become a direct citation | Bas: no need, isn't confusing |
| **178IIIa** | the *solution* used one-sided distributivity, not an effect-monoid axiom | the four-fold law of 178II leaves exactly the terms to vanish; note one-sided distributivity *is* derivable, but only **after** `a ⊙ 0 = 0`, which is what the exercise asks for — so the solution as written was circular, not merely under-justified | Bas: exercise fixed, no explivit errata as exercises aren't in the original book. **FIXED upstream** (`ecc8bdc`): the solution now argues from the four-fold law, as `exc_emonzero` does |
| **178IIIa** | the exercise read "Show that `a ⊙ 0 = a = 0 ⊙ a`" | typo: `a ⊙ 0 = 0 = 0 ⊙ a` (as printed it is false in `[0,1]`) | Bas: fixed with errata. **FIXED upstream** (`11e51c9`, erratum `exc-emonzero`); our `exc_emonzero` already carried the corrected form |
| **178IIIa/178IV** | the unnumbered point 40 ("Later, in a tangent, we will need the following specific fact about effect monoids") is `\begin{point}`-nested **inside** the exercise point 31, so it displays as a sub-point of `exc-emonzero` instead of as 178IV introducing 178V | move the `\end{point}` of point 31 before point 40 | Bas: fixed |
| **179III.2** | Gudder representation is asserted by citation only | nothing to transcribe — parked | Bas: why should this an errata?  |
| **181IV.1** | the `⇐` direction (`eff.tex:1030`) goes through the four-fold sum `[f,0] ⋁ [0,g] ⋁ [f',0] ⋁ [0,g']`, which tacitly needs the generalized (permutation) associativity of **174IV** — a result the thesis states without proof | the step can be avoided: apply the compatible-sum axiom directly to `[(κ₁∘f) ⋁ (κ₂∘f'), (κ₁∘g) ⋁ (κ₂∘g')] : X+Y ⟶ Z+Z`, as formalized | Bas: formalize the generalized permutation associativity and proof with that. It's kind of tedious but obvious hence not written out in the text. |
| **181XIII** | the proof (`eff.tex:1181`) opened its chain with "`1 = 1 ∘ 1`", which is **not available at that point** — that `1 ∘ 1 = 1` is a *consequence* of the computation that follows, so the step is circular | it is also unnecessary: PCM-enrichment on `id ⋁ idᵖ = 1` gives `1 ⊥ (1 ∘ idᵖ)`, so the zero–one axiom yields `1 ∘ idᵖ = 0`, hence `idᵖ = 0` and `id = 1` | Bas: added errata. Just removed the 1=1 \odot 1 step. Rest holds. **FIXED upstream** (`334a383`, erratum `one-m-is-id`); the printed proof now runs exactly as `one_m_is_id` does |
| **183III.1** | the solution (`bsols.tex:1711`) dropped two `=` signs: "Clearly `g ∘ f ∘ δ g ∘ γ = α`" and "`k ∘ δ' ∘ β`" | the argument itself transcribes unchanged | Bas: fixed. **FIXED upstream** (`2be3ceb`); no Lean change (our doc comments never quoted the garbled text) |
| **186IV** | states the pullback square only in its `▷₁` form, but 186X's proof needs `▷₂` too | state both | Bas: Isn't it obvious that would hold as well? |
| **186VIII.2** | uses a `κ₂` form of a pullback axiom stated only for `κ₁` | — | Bas: Isn't it obvious that that would hold as well? |
| **187VI** (`eff.tex:1885`, the zero–one axiom) | the axiom `1 ⊥ p ⟹ p = 0` is derived from a pullback square drawn between `1`, `1`, `(1+1)+1` and `1+1`, cited as "the following instance of the right pullback square of (pullbacks)". Read in `Par C` the corners are wrong (a `par-pullbacks` square has corners `Par(A+X), Par(B+X), Par A, Par B`, none of which is `Par 1` for the top row); read in `C` the square `(id, κ₁∘κ₁; κ₁, [id,κ₂])` is not an instance of `tot-pullbacks` either. The diagram mixes Par-objects with their `C`-representations and we could not reconstruct it | **no defect in the result**: the axiom follows in three lines from material proved two paragraphs earlier in the same point. With `s = 1 ⋁ p`, partial associativity turns `s ⋁ s^⊥ = 1` into `1 ⋁ (p ⋁ s^⊥) = 1`, so `p ⋁ s^⊥ = 1^⊥ = 0` by the *uniqueness* of the orthosupplement; the bound `d` for `p ⊥ s^⊥` then has `1 ⊙ d = 0`, hence `d = 0` by **186VIII.2** and `p = ▷₁ ⊙ d = 0`. This is how it is formalized | Bas: you're looking at the wrong pullbacks. This refers to the defining pullbacks of an effectus. It's not the square directly, but an isomorphism is applied in the bottom right corner. Take X=Y=B=1, A=1+1,  f=!, and g=!. Seems obvious? |
| **189I.2** | the solution swapped "total form" and "partial form" | — | Bas: fixed. **FIXED upstream** (`4879d04`, `bsols.tex:1804`): now "`0` is final in an effectus in *partial* form" / "strict initial in an effectus in *total* form", which is what `distinction_part_tot_eff_1/_2` prove |
| **191VIII** (`exc-rng-eff`, the joint-epicity paragraph of the solution, `bsols.tex:1928`) | the closing chain was garbled: "`f(k,l,m) = f(k,0,0) = f(0,l,m) + f(0,0,m) = g(k,0,0) = g(0,l,m) + g(0,0,m) = g(k,l,m)`".  As printed the first equality is false (`f(k,l,m) ≠ f(k,0,0)` in general) and the `=`s chain three unrelated expressions | the intended identity is `f(k,l,m) = f(k,0,0) + f(0,l,0) + f(0,0,m)`, and likewise for `g`; with the three summands compared one by one the argument is correct and is how it is formalized | Bas: fixed. ⚠ **the fix is incomplete** (`15e890c`): the `=`s are repaired to `+`s, but the middle summand still reads `f(0,l,m)`/`g(0,l,m)` where the paragraph's own two preceding lines establish `f(0,l,0) = g(0,l,0)`; it should be `f(0,l,0) + f(0,0,m)`.  As printed the `m` is counted twice |
| **192V.3.3** | "semilattices are exactly abstract 2-convex sets" is **false**: by Def. 192II the support uses the *partial* sum and `1 ⋁ 1` is undefined in `2`, so `𝒟_2 ≅ Id` and they are just sets | holds for the non-empty-finite-powerset monad; see QUESTIONS.md B1 | Bas: fixed. **FIXED upstream** (`bb9615f`, erratum `eff-semilattice-aconv`): the claim is deleted, the surviving direction relabelled.  Our side realigned in session 39 — `two_convex_nonempty`/`two_convex_unique` (`AConv₂ ≅ Set`), `semilattice_unitInterval_convex` with its structure map pinned, and `semilattice_cancellative_iff` |
| **193IX** | the *existence* half ("every `z ∈ X+Y` is `h(⋁λᵢ\|κ₁xᵢ⟩ ⋁ ⋁σⱼ\|κ₂yⱼ⟩)`") is read off the explicit construction of the coproduct | **no defect**: it follows from the universal property alone — cut `X+Y` down to the image of `𝒟_M(X+Y)`, which is closed under `h` and again receives `κ₁,κ₂`, so the corestriction splits the inclusion. Formalized this way, so 194I.4 needs neither the construction nor the derivation calculus | Bas: please formalize the derivation calculus too just to see if it was correct. |
| **194I** | "As `𝒟_M ∅ = ∅`, the empty set is trivially also an abstract `M`-convex set and in fact the initial object of `AConv_M`" is **false for the trivial effect monoid** `M` (`1 = 0`): there the only formal convex combination is the empty one, so `𝒟_M ∅` is a *singleton*, admits no `h : 𝒟_M ∅ → ∅`, and `∅` is not an object of `AConv_M` at all | the proposition survives — for `1 = 0` every object of `AConv_M` is a singleton, so `1` is initial; add the case split (or assume `1 ≠ 0` for effect monoids). Formalized with the split | Bas: fixed. **FIXED upstream** (`d61feea`, erratum `aconvalmosteffectus`): the printed proof now does the `M = 1` case split first, which is the shape ours already had — no Lean change.  This answers QUESTIONS B7: effect monoids stay possibly trivial |
| **194I** | the injectivity of `κ₁ : X → X+Y` (eff.tex:3057–3175) is proved by an induction over *derivations* — the longest argument of parsec 194, needing 193IX, 193IV and 178V | **no defect, but it can be deleted**: constant maps between abstract `M`-convex sets are affine (`𝒟_M(const z)(φ) = η(z)`, since the coefficients sum to `1`), so for `X ≠ ∅` the cotuple `[id_X, const x₀] : X+Y → X` is a **retraction** of `κ₁`, and for `X = ∅` there is nothing to prove. Point 50 of parsec 1940 could be replaced by two sentences | Bas: do formalize the proof to check it. |
| **194I** | eff.tex:3173 had an unbalanced parenthesis: `$\varphi_{ij} = (\mathcal{D}_M \kappa_1)(\chi))$` | drop the last `)` | Bas: fixed (`17cde5c`) |
| **195VII** | rested on a false step: "if `c⊙a ⊥ c⊙b` then `(c/c)⊙a ⊥ (c/c)⊙b`" fails in `[0,1]` at `c = ½`, `a = b = 1` | statement is true; our proof takes another route | Bas: new proof. **FIXED upstream** (`5a3fafd`, erratum `eff-divisoid-add`): the false lemma is deleted and the printed proof is now the route `divisoid_div_ovee` already took — special case `c = a ⋁ b` via `(a⋁b)/(a⋁b) ⊖ a/(a⋁b)`, then multiply by `(a⋁b)/c`.  Divergence class 1 (faithful) as of session 39 |
| **196II** | "assume `α : Z → X+1` and `β : Z → Y+1`" (eff.tex:3397–3398) — `α` and `β` are **swapped** relative to the diagram and to every later use (`α(z) = h₁(λ₀\|κ₁(·)⟩ ⋁ ⋯)` lies in `1+Y`), and `Y+1` is not an object in play | read "`α : Z → 1+Y` and `β : Z → X+1`"; the rest of the sentence, and the equation `(!+id)∘β = (id+!)∘α`, are then correct | OPEN (nit) |
| **196II** | in point 50 the quotient maps are declared as `q₁ : 𝒟_M(X+Y) → X+Y`, `q₂ : 𝒟_M(1+Y) → 1+Y`, `q₃ : 𝒟_M(X+1) → X+1`, but every later use numbers them like `h₁,h₂,h₃`, i.e. `1+Y`, `X+1`, `X+Y` (`α(z) = q₁(φ)`, `β(z) = q₂(ψ)`, `γ(z) = q₃(χ)`, `γ(z) = q₃(χ) = h₃(⋯)`). In the same display `φ ∈ 𝒟_M Y` and `ψ ∈ 𝒟_M X` should be `𝒟_M(1+Y)` and `𝒟_M(X+1)`, and the two conditions `χ(κ₁(y)) = φ(κ₁(y))`, `χ(κ₂(x)) = ψ(κ₂(x))` have `κ₁` and `κ₂` interchanged (in `X+Y` it is `κ₂` that takes a `y`) | renumber the declaration to `q₁ : 𝒟_M(1+Y) → 1+Y`, `q₂ : 𝒟_M(X+1) → X+1`, `q₃ : 𝒟_M(X+Y) → X+Y` — matching `h₁,h₂,h₃` — retype `φ`, `ψ`, and swap the two `κ`s (three times: in the display, in the choice of `φᵢ,ψᵢ,χᵢ`, and in the computation of `χ(κ₁(y))`). With those changes the paragraph is correct as written | OPEN |
| **196II** | the well-definedness of `γ` is proved by interleaving **two** derivations `Φ_i`, `Ψ_i` into one `Ω_i` (eff.tex:3446–3590, two pages), which needs the syntactic description of the least congruence that **193IV** leaves to the reader | **no defect, but the whole construction can be deleted.** The normalization the proof already performs *inside* `ω` — divide by `λ₀`, correct with `r = (λ₀/λ₀)ᵖ` — applied to a whole combination shows that over an effect divisoid **every** element of `X+Y` is a *binary* mixture `λ\|κ₁x⟩ ⋁ λᵖ\|κ₂y⟩`. Given that, well-definedness is two applications of "constant maps are affine": `[κ₁, const κ₂y']` and `[const κ₁x, κ₂]` transport the equalities in `X+1` and `1+Y` back to `X+Y`, and the `λ`s agree because the mass map `X+Y → 1+1` factors through both legs. Formalized this way (`AConvMCat.exists_mix`, `AConvMCat.coprod_jointly_injective`) | OPEN (informational) |
| **196II** | point 50 (eff.tex:3592–3657) proves affineness of `γ` by hand, choosing representatives `φ,ψ,χ` and pushing them through `μ` | **no defect, but it is free**: `γ` is *characterized* by `(id+!)∘γ = β`, `(!+id)∘γ = α`, and those two legs are jointly injective (the uniqueness half, proved just above), so affineness of `γ` follows from affineness of `α` and `β` in three lines — `(id+!)(γ(h_Z p)) = α(h_Z p) = h(𝒟_M α (p)) = (id+!)(h(𝒟_M γ (p)))`, and likewise for `(!+id)` | OPEN (informational) |
| **208III** | the proof takes `s ⋁ t = s ∨ t` from `ea-modularity-prop` (177Ia, **false as printed** — see the 177Ia row) and orthomodularity from `orth-ea-is-orthomodular` (177VI, whose printed proof also cites 177Ia). Both citations are to the *existence* half, which is the false one; here they happen to be applied where the join is already known to exist (`SPred X` is a lattice), so the argument is repairable in place, but as printed it appeals to a false proposition | both detours are avoidable: sharpness alone gives `s ⋁ t ≤ s ∨ t` (a predicate is `≤` a sharp `j` iff it vanishes on `π_{jᵖ}`), and the orthomodular law follows from `ovee_le_of_le` + 208IX/208XII — formalized this way, which is what removed the last `sorryAx` leak from B/Eff | OPEN |
| **213I** | the proof of `μ/μ = ⌈μ⌉` runs "`λ' ∘ ζ_⌈μ⌉ ∘ μ = μ = 1 ∘ ζ_⌈μ⌉ ∘ μ` as `IM μ = IM asrt_μ = ⌈μ⌉`; by epicity of `ζ_⌈μ⌉ ∘ μ` we see `λ' = 1`" | **no defect, but both steps are already available.** `1 ∘ (ζ_⌈μ⌉ ∘ asrt_μ) = μ` is part of the conclusion of **212I** itself (a quotient `ξ` for `p` satisfies `1 ∘ ξ = pᵖ`, here `p = μᵖ`), so the image computation is not needed; and "epicity" is the uniqueness half of the very universal property the proof invokes two sentences later.  The paragraph collapses to two lines, which is how it is formalized (`scalDiv_self`) | OPEN (informational) |
| **219II–219XVI** | `dagger-is-functor` is proved by fixing the Setting `dagger-setting` (219II: the four isomorphisms `χ, ω, β, α` putting `f ∘ g` in standard form piece by piece) and then chaining **eight** results — 219III, 219V and the "daggered squares" 219VII, 219IX, 219X, 219XIII, 219XIV | **the Setting and six of the eight are avoidable.** `(f ∘ g)† = g† ∘ f†` follows from **218X** (`f = h ∘ asrt_{1∘f}` with `h` pristine), **219XI** (`(asrt_b ∘ asrt_a)† = asrt_a ∘ asrt_b`) and **219XIV** alone, *provided* 219XIV is stated in the transport-free general form `(ζ_e ∘ asrt_p ∘ π_t)† = ζ_t ∘ asrt_p ∘ π_e` for **any** sharp `t`, `e` with `⌈p⌉ ≤ e` (the thesis proves exactly this, but only for `e = ⌈p⌉` and only for the `χ` of its Setting; the proof is unchanged).  The point is to peel `f ∘ g` one generator at a time instead of putting the whole composite in standard form at once: the daggers of `θ ∘ f` (`θ` an iso), `π_i ∘ f` and `ζ_s ∘ f` are one comprehension- resp. quotient-uniqueness argument each — the technique of 219VII and of the published solution to 219IX — and `asrt` is 219XI.  `ω`, `β`, `α`, 219III, 219V, 219VII, 219X and 219XIII are then never needed; what survives of the Setting is exactly the `α₂`, `β₂` pair *inside* 219XIV's own proof.  This is how 219XVI, and with it 220II and 215III, are formalized | OPEN (informational) |
| **221II** | in the definition of a dilation, the mediating map is typed `h' : X → P` | must be `X → P'`, or `ϱ' ∘ h'` does not typecheck | OPEN |
| **221IV.6** | never checks that the mediating map is *pure*, though 221II requires it | true; we have now proved it | OPEN |
| **225VI** (S3) | the proof (`eff.tex:7415`) argues `andthen p q = 0 ⟹ asrt_q ∘ asrt_p = 0 = asrt_0`, applies the dagger, and concludes — which needs `asrt_p† = asrt_p` and functoriality of the dagger | **no defect**: the dagger can be avoided entirely, using only the `asrt_sq` axiom of a †-effectus plus uniqueness of square roots. (S2)'s "obvious" `andthen 1 p = p` is likewise exactly the second half of the absorption rule 211XV at `f = p`, `t = 1` | OPEN (informational) |
| **226II** | the proof builds a pristine `l` and computes `asrt_{s&tᵖ}² = asrt_{s&tᵖ}` to show `s & tᵖ` is sharp — but the hypothesis gives `tᵖ ≤ s`, so **213V** yields `s & tᵖ = tᵖ` in one step | cite `simple-andthen-absorption` instead; the rest of 226II then needs no dagger at all | OPEN (nit, but it shortens a page) |
| **226VII** | uses `m†` and "comprehensions are †-mono" / "`m†` is sharp" | the `ζ` of 211VII does the same job without the dagger development | OPEN (nit) |
| **227III.1** | exactness at `B` given as `IM^⊥ f = ⌈1∘f⌉` — does not mention `g` at all | must be `⌈1∘g⌉`; our corrected form is proved and the printed one is false | OPEN |
| **228III–228VI** (`eff.tex:7820–8028`, the overview and `snakedidents`) | the proof builds the *right* subquotient (`v = ξ_{h_⋄(IM a)}`, the comprehension `h'` with `v ∘ h = h' ∘ a_ζ`), proves `h_⋄(1) = (v^□ ∘ h'_⋄)(1)` (`snakehdiamondone`) and the second and fourth forms of `snakedidents` (`d_⋄ = h'^□ ∘ v_⋄ ∘ …`), and needs the exchange identity `g^□ ∘ (c_π)_⋄ = m_⋄ ∘ g'^□` for the left face | **none of that is needed.**  The four exactness statements only ever evaluate `d_⋄` at `1` and `d^□` at `0`.  At `1` the first form of `snakedidents` suffices and the exchange identity degenerates to `IM m = g^□(c^□(0))`, which is the definition of `m`; and `d^□(0)` follows from `g'_⋄ ∘ g'^□ = id`, `b'^□ = m^□ ∘ b^□ ∘ h_⋄` and injectivity of `(c_π)_⋄`.  So `v`, `h'`, `snakehdiamondone` and three of the four forms of `snakedidents` are avoidable, and with them the whole right-hand face of the cube.  This is how `snake_lemma` is formalized | OPEN (informational) |
| **228IV** (`eff.tex:7851`, the construction of `h'`) | the line "`(v ∘ h)^⋄(1) = h^⋄(h_⋄(IM a)) = IM a = 1 ∘ a_ζ`" is wrong in its two middle terms: the operator that cancels `h_⋄` is `h^□`, not `h^⋄` (`π^□ ∘ π_⋄ = id` is **227V**; `π^⋄ ∘ π_⋄` is not the identity), and the result of that cancellation is `IM a`, whose *complement* is `1 ∘ a_ζ` | the chain should read `(v ∘ h)^⋄(1) = h^□(h_⋄(IM a))^⊥ = (IM a)^⊥ = 1 ∘ a_ζ` — equivalently, and more directly, `(v ∘ h)^□(0) = h^□(h_⋄(IM a)) = IM a = a_ζ^□(0)`.  The outer equality, which is all the proof uses, is correct as printed | OPEN |
| **228VIII** (`eff.tex:8074`, exactness in `cok b`) | the display opens `h̄_⋄(1) = (h̄_⋄ ∘ (a_ζ)_⋄ ∘ a_ζ^□)(1) = (h̄_⋄ ∘ (a_ζ)_⋄)(1) = ((b_ζ)_⋄ ∘ h_⋄)(1)`, the last step "by dfn. `h̄`".  The first two steps are vacuous — `a_ζ^□(1) = 1` and `(a_ζ)_⋄(1) = 1`, quotients being epic — so all the content sits in the third, and it is *not* the definition of `h̄`: `a_ζ ∘ h̄ = (b_ζ ∘ h) ∘ (a_ζ^† ∘ a_ζ)` and `a_ζ^† ∘ a_ζ = asrt_{(IM a)^⊥} ≠ id` | what the step needs is `(b_ζ)_⋄(h_⋄((IM a)^⊥)) = (b_ζ)_⋄(h_⋄(1))`, which holds because `h_⋄` and `(b_ζ)_⋄` preserve joins, `(IM a) ∨ (IM a)^⊥ = 1`, and `h_⋄(IM a) = b_⋄(IM f) ≤ IM b = ker b_ζ` (the commuting square), so the `IM a`-summand is killed.  This is how it is formalized | OPEN |

## Thesis B — `dils.tex` / `bsols.tex`

| # | defect | fix | status |
|---|---|---|---|
| **139VII** | finishes by citing an external source (`westerbaan2016universal`, lem. 9) | we found a citation-free substitute reusing the author's own identity | OPEN (nit) |
| **140X.1** (`paschke-basics`, `dils.tex`) | "Show `(𝒜, ϱ, id)` is a Paschke dilation of an nmiu-map `ϱ : 𝒜 → ℬ`" — the dilating algebra is wrong: a Paschke dilation of `φ : 𝒜 → ℬ` is a triple `(𝒫, ϱ : 𝒜 → 𝒫, h : 𝒫 → ℬ)`, so with `𝒫 = 𝒜` the map `id` would have to be `𝒜 → ℬ` | `(ℬ, ϱ, id)`.  Our `paschke_basics_1` already carries the corrected form and is proved | OPEN |
| **140X.2 solution** (`paschke-basics`, `bsols.tex`) | `ϱ'` is typed `𝒜 → 𝒫'`, so the standing hypothesis `h' ∘ ϱ' = h` does not typecheck and the later `σ ∘ ϱ' ∘ ϱ = ϱ` is meaningless | `ϱ' : 𝒫 → 𝒫'`.  The argument itself is correct and is what our Lean proof transcribes.  In the same solution: item 1 types the mediating map `σ : 𝒫 → ℬ` where it is `𝒫' → ℬ`, and item 3 drops the primes on `h'ᵢ` in two displays | OPEN (nit) |
| **152XII** (`dils.tex`, the "bounded order completeness" half of 152X's proof) | opens "Let `(T_α)` be a **norm-bounded** net of self-adjoint elements of `𝒷ᵃ(X)`", but what **42I**.1 asks for is a directed set that is bounded *above*, which need not be norm-bounded (`{-n : n ∈ ℕ}` in `ℝ`); as printed the proof does not cover the case the definition requires, and the constant `r` with `‖T_α‖ ≤ r` that the bound on `B` uses is not available | one sentence: a directed `D` bounded above by `u` may be replaced by its cofinal tail `{d ∈ D | d₀ ≤ d}`, which has the same upper bounds and is norm-bounded (`0 ≤ d − d₀ ≤ u − d₀`).  Our proof instead bounds `B(x,x)` between `⟨x,d₀x⟩` and `⟨x,ux⟩` directly, which needs no `r` at all.  The identical gap sits in `vn.tex` **44XV**, where the same cofinal-tail step had to be supplied | OPEN |
| **153I** (`hilbmod-ad-ncp`, `dils.tex`, Proposition, second half) | "If `X` and `Y` are self-dual, then `ad_T` is normal" assumes more than the proof needs: normality of `ad_T : 𝒷ᵃ(Y) → 𝒷ᵃ(X)` is preservation of suprema *of `𝒷ᵃ(Y)`*, and the suprema in `𝒷ᵃ(X)` it has to produce are given by hypothesis | "If `Y` is self-dual"; our `hilbmod_ad_ncp` keeps the thesis's `hX` (faithful transcription) but does not use it, and says so in its doc comment | OPEN (nit) |
| **153IV** (`hilbmod-adj-vector-ncp`, `dils.tex`, and its `bsols.tex` solution) | both open with "Let `𝒜` be a C\*-algebra", but the conclusion asked for is that `φ` is an **n**cp-map, and normality is only defined over a von Neumann algebra; the solution's own first step ("`𝒜` is a self-dual Hilbert `𝒜`-module", and the appeal to `hilbmod-ad-ncp` part 2) needs `𝒜` to be one | "Let `𝒜` be a von Neumann algebra".  Our `hilbmod_adj_vector_ncp` carries the corrected hypothesis and is proved.  The same omission sits one level up in **153I** `hilbmod-ad-ncp`, whose normality half cites `hilbmod-vecstates-normal` (152XIII) and so needs `𝒷` to be a von Neumann algebra, though the Proposition says only "Hilbert `𝒷`-modules" | OPEN |
| **157III** (`dils.tex`, Definition) | `φ_t = h(t ϱ(a))` leaves `a` unbound | `φ_t(a) = h(t ϱ(a))` | OPEN (nit) |
| **158V** (`kaplansky-hilbmod`, `dils.tex`, the "Ultranorm continuity `h`" step, incl. the right-hand half of `kaplanskytodo2` and all four of `A₁, A₁', A₂, A₂'`) | **false as printed**: `h` is *not* ultranorm continuous, not even on norm-bounded sets, so `kaplansky-splitting` does not converge.  Counterexample in `ℬ = B(ℓ²)`, `X = ℬ`: `y = \|e₂⟩⟨e₁\|`, `y_n = \|e₂⟩⟨e₁+e_n\|` (`n ≥ 2`).  Then `⟨y_n−y, y_n−y⟩ = p_n` and `ω(p_n) → 0` for every np-functional (normality), so `y_n → y` ultranorm with `‖y_n‖ = √2`; but for the vector functional `ω₀ = ⟨e₁, · e₁⟩` and *every* `n`, `ω₀(A₁) = −1/12`, `ω₀(A₁') = −1/18`, `ω₀(A₂') = 1/6` and `ω₀(⟨h(y)−h(y_n), h(y)−h(y_n)⟩) = 1/9`.  The precise failure is the RHS of `kaplanskytodo2`, the one whose "different, but simpler" proof is omitted: `⟨y, y_n−y⟩(1+⟨y_n,y_n⟩)⁻¹` has `ω₀`-value `−1/3`.  (Its LHS is fine — Cauchy–Schwarz against `y_α(1+⟨y_α,y_α⟩)⁻¹`, of norm `≤ 1`.) | unknown; the omitted RHS argument would need `⟨y,y_α−y⟩ → 0` in the *mirrored* ultrastrong topology (`ω(bb*) → 0`), which module Cauchy–Schwarz does not give — it gives `ω(b*b) → 0`, and "ultrastrongly null times bounded is ultraweakly null" is false on that side (`\|e₁⟩⟨e_n\| · \|e_n⟩⟨e₁\| = \|e₁⟩⟨e₁\|`).  **158II itself is presumably still true** — its entourage form lets one *choose* each approximant, rather than take an arbitrary ultranorm-convergent net — but it needs a different proof.  Our `kaplansky_hilbmod_A₁/A₁'/A₂/A₂'` therefore keep their `sorry`s, with the counterexample in the doc comment above them | OPEN (**needs an author decision**) |
| **158V** `kaplansky-splitting` (`dils.tex`, the displayed identity) | off by a factor `4`: with `h(y) = y·2/(1+⟨y,y⟩)` the left-hand side carries the square of that `2`, while none of `A₁, A₁', A₂, A₂'` does.  On the counterexample of the row above, `⟨h(y)−h(y_n),h(y)−h(y_n)⟩ = 1/9` at `ω₀` and `A₁+A₁'+A₂+A₂' = −1/12 − 1/18 + 0 + 1/6 = 1/36` | `⟨h(y)−h(y_α), h(y)−h(y_α)⟩ = 4(A₁+A₁'+A₂+A₂')`, or absorb the `4` into the definitions of the four terms.  Harmless for the argument as printed (all four are claimed to vanish), and independent of the falsity above | OPEN (nit) |
| **159IV** (`ketbra-ultraweakly-dense`, `dils.tex`) | "Let `X` be a self-dual Hilbert `ℬ`-module … If `(eᵢ)` is an orthonormal basis of `X`" — the self-duality hypothesis is redundant: by **149XI** (the (4) ⇒ (1) half of `dils-selfdual`) a pre-Hilbert `ℬ`-module with an orthonormal basis *is* self-dual | drop it, or say it is implied.  Our `ketbra_ultraweakly_dense` keeps the thesis's `hX` (faithful transcription) and is **proved without using it** — the linter suppression above the declaration is the evidence | OPEN (nit) |
| **165III** | needs positivity of `Mₙ(⊗)` (`cp-bilinear`), which the definition of a vN tensor product does not provide | **no defect after all**: the positivity is *derivable* from miu-bilinearity, and 165III is now **proved** (`dfn_tensor_of_hilbmod_maps`, 2026-08-14) from `Theses.A.CStar.matBilin_nonneg_of_mi` (relocated out of `A/Proc`) plus **33II**.1 and `ExtTensor.univ`.  Two small gaps in **165IV**'s own write-up, both harmless: the displayed chain needs `Mₙ(⊗)` to be ℂ-homogeneous in its *second* slot, which the `tensor` definition supplies but our `IsVNTensor` does not record — it is derivable (`vnTensor_smul_complex_right`); and "`S ⊗ T ∈ 𝒷ᵃ(X ⊗ Y)`" is asserted without an adjoint, which is `S* ⊗ T*` by the same construction | CLOSED (informational) |
| **166II** (`ultranorm-continuity-ext-tensor`, `dils.tex`, proof) | "The maps `b ↦ 1 ⊗ b` and `a ↦ a ⊗ 1` are ncp `% by \TODO{},`" — the justification is an unfinished `\TODO` left commented out in the source.  Complete positivity is free (both legs are miu-maps, so `cp-of-mi` applies), but the proof uses **normality** as well, to turn `⟨xα−x, xα−x⟩ → 0` ultraweakly in `𝒜` into `⟨xα−x,xα−x⟩ ⊗ 1 → 0` ultraweakly in `𝒜 ⊗ ℬ`, and that is the half the `\TODO` was to supply | supply the reference (proc.tex 108II ff.).  **Supplied on our side and no longer blocking**: `vnTensor_legLeft_normal` / `vnTensor_legRight_normal` (`B/Dils/SelfDual.lean`) derive normality of both legs from the *faithfulness* of the product functionals alone — no extra clause on `IsVNTensor` is needed (QUESTIONS.md **B5**) — and **166II is now proved** (`ultranorm_continuity_ext_tensor`, 2026-08-14).  The `\TODO` is still a hole in the printed proof | OPEN (thesis side) |
| **166II** (`ultranorm-continuity-ext-tensor`, `dils.tex`, statement) | "If `x_α → x` and `y_α → y` ultranorm for **norm-bounded nets** `x_α` and `y_α`" — only **one** of the two boundedness hypotheses is needed.  The proof splits `x_α ⊗ y_α − x ⊗ y = (x_α − x) ⊗ y_α + x ⊗ (y_α − y)`, in which `x_α` is never bounded; it is used only to make `⟨x_α−x, x_α−x⟩ ⊗ 1` an *effect*, which is an artefact of routing the estimate through **44III** `vanishing-effects`.  Estimating in the order of `𝒜 ⊗ ℬ` instead — `Ω(⟨d,d⟩ ⊗ ⟨y_α,y_α⟩) ≤ M²·Ω(⟨d,d⟩ ⊗ 1)`, where `Ω(· ⊗ 1)` is an np-functional by leg normality — removes it.  Splitting the other way (`x_α ⊗ (y_α − y) + (x_α − x) ⊗ y`) drops the bound on `y_α` instead | hypothesise a bound on one net only, or leave both for symmetry and say so.  Our `ultranorm_continuity_ext_tensor` keeps both (faithful transcription) and is **proved without using `hxb`** — the linter suppression above the declaration is the evidence | OPEN (nit) |
| **169XI.2** (`dils-filter-basics-exercise`) | "there is a unique unital ncp-map `φ'` with `φ = c ∘ φ'`" names the wrong filter: `c : ℬ → 𝒞` is the filter of part 1, the one in play is `c' : 𝒞' → ℬ`, and as printed the composite does not typecheck | `φ = c' ∘ φ'` | OPEN |
| **169VIII** (`dils-def-filter`, `dils.tex`) | "an ncp-map `c : 𝒜 → ℬ` is a filter for `b ≥ 0` **if `c(1) ≤ b`** and every ncp-map `f : 𝒞 → ℬ` with `f(1) ≤ b` factors uniquely through `c`" — this is *not* the definition it says it is recalling.  proc.tex **96I** (`filter`) asks the universal property for `f(1) ≤ c(1)` and calls `c` a filter *for `c(1)`*, so there `c(1) = b` by construction.  With `≤` the two differ: `c = ½·id : ℬ → ℬ` satisfies **169VIII** for `b = 1` (every `f` with `f(1) ≤ 1` factors uniquely, as `f' = 2f`) while `c(1) = ½` | `c(1) = b`.  Note **169VIII**'s derived notion "`c` is a filter" (= a filter for *some* `b`) is unaffected — a `≤`-filter for `b` is an `=`-filter for `c(1)` and conversely — so **170I** (purity), **169XI**.1 and **169XII** are untouched; only the *for `b`* phrase, used in **169XI**.2, changes | OPEN |
| **169XI.2 solution** (`bsols.tex`) | the step "`c'(φ'(1)) = φ(1) = c'(1)`" uses `c'(1) = φ(1)`, which Definition **169VIII** gives only as `c(1) ≤ b` | this is the row above, and the repair suggested here in an earlier revision — "the isomorphism with the standard filter built in the `dils-filters-injective` solution gives `c(1) = b`" — **does not work**: that argument gives `c = c_b ∘ ϑ` for an ncp-*isomorphism* `ϑ`, and `ϑ(1) = 1` is exactly what fails (`ϑ = ½·id` in the counterexample above, `c(1) = ½·b`).  With **169VIII** repaired to `c(1) = b` the step is immediate and the exercise is true; as printed, part 2 is **false** — `φ = id : ℂ → ℂ`, `c' = ½·id` admits no unital `φ'` with `c' ∘ φ' = φ`.  Our `dils_filter_basics_2a` transcribes **169VIII** literally and therefore stays `sorry` | OPEN |
| **170IV solution** (`surjective-nmiu`, `bsols.tex`, converse half) | the two mediating maps are introduced garbled — "by the universal property of `h` there is a unique ncp-map `ϑ₁ : z𝒜 → 𝒞` with `h ∘ ϑ₁ = ϑ₂`" (`ϑ₂` is not yet defined, and the universal property of `h` produces maps *out of* `𝒞`, not into it), and dually for `ϑ₂` | intended: `ϑ₁ : z𝒜 → 𝒞` with `ϑ₁ ∘ h_z = h`, from the universal property of `h_z`; and `ϑ₂ : 𝒞 → z𝒜` with `ϑ₂ ∘ h = h_z`, from that of `h`.  Also "isomoprhism" ×2, "seperate" ×1 | OPEN (nit) |
| **`onb1` solution** | gratuitously assumes self-duality, which neither the exercise nor the statement needs | — | OPEN (nit) |

---

## How these were found

Worth recording, because it shaped the method: **proving a statement finds
nothing.**  Every erratum above came from *comparing* our proof with the
author's.  Two dedicated audits — re-reading the authors' arguments for
statements already proved from Mathlib — produced **10 errata between them**,
after which the workflow was inverted to "transcribe the thesis's proof first,
Mathlib only as fallback".

The single most productive check is reading a declaration against the
`file:LINE` its own doc comment carries.  The second is reading it against its
own doc-comment *prose*, which has caught a false statement with no reference to
the source at all.

## What does *not* belong here — and how three items got in anyway

**2026-08-14, prompted by Bas reading 175II.4 and observing it points out no
error.**  He was right, and the audit it triggered found two more.  All eight
items of a single bundled row — filed as "assorted session-1 findings, see
PROVING-LOG for each" — had never been individually vetted:

* **175II.4** — an observation about *our* Lean proof (that the orthomodular
  law is genuinely needed for `orth_unique`, not decoration).  No claim about
  the thesis.  **Removed.**
* **195V.1** — a repair to *our* `EffectDivisoid` instance, and already filed
  in PROVING-LOG under the heading "Repairs to **our** formalization (not
  thesis errata)".  **Removed.**
* **211XVI** — about *our* doc comment being out of date.  **Removed.**
* **225VI** — explicitly recorded as "not an erratum, but a *shorter route*".
  **Kept, relabelled `OPEN (informational)`**, which is the class this file
  already uses for "the thesis proves more than it needs".
* **175V.7** — real, but a **duplicate** of the unnumbered `eff.tex:414` row.
  **Merged**, and the row now carries its point number so it sorts correctly.
* **181IV.1, 181XIII, 183III.1** — genuine, and now have their own rows with a
  stated defect, as every other row does.

The lesson is structural, not clerical: **a row without a per-item defect
description is where a non-defect hides.**  The format of this file — one line
naming the defect, one naming the fix — is itself the check.  Anything that
cannot be written in that form is, by that very fact, not an erratum: it is a
note about our formalization (→ PROVING-LOG) or a decision (→ QUESTIONS).
